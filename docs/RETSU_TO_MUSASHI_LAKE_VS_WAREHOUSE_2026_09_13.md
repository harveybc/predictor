# Retsu → Musashi — lake vs warehouse in data-gov

Canonical copy (edit there if you change the text):

`data-gov`: `docs/RETSU_TO_MUSASHI_LAKE_VS_WAREHOUSE_2026_09_13.md`

Harvey: one governance kernel; **lake** = schema-on-read files (`financial-data`); **warehouse** = schema-on-write cube (OLAP). UI/`kind`/docs only. Do not rename `datagov.lake` until G5 is green. Do not split the product.
