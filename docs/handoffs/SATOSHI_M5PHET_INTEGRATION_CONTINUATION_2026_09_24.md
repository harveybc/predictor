# Integration continuation: implement, verify, keep independent work moving

Read [the source/API review](../audits/work_plan/MUSASHI_M5PHET_INTEGRATION_REVIEW_2026_09_24.md)
of predictor `5c2dc332` and M5PHET `adba176`. This adds precise acceptance work to
the existing assignment; it is not a new research campaign or global hold.
Satoshi codes. Musashi reviews independently. Existing limits remain in force.

## 1. Experimental lane, independent of M5PHET repair

First inspect actual leases/processes, remaining budgets and child revisions;
do not launch duplicates from a chat snapshot. Keep the admitted corrected
contrast running unchanged. Finish B's existing closure owner, if not already
done. Preserve the L=512 versus paper lookback-search qualification; report
same-row naive and author metrics, not an unsupported exact-protocol claim.

Repair predictor's regime summary in a separate worktree: require exact seed x
regime population, typed nonmissing evidence and explicit incomplete/invalid
states. Missing detector-change evidence cannot be True for R2; unexpected cells
cannot expand the denominator. Add finite selected/restored loss and selected
checkpoint checks at the acceptance boundary, including post-restoration detector
identity. Prove missing/bad checkpoint and mismatched restored score refuse.
Re-evaluate the existing completed cells and replay their selected weights in a
fresh process before acceptance, not automatic retraining. Score the declared
complete population with the reference metric and naive; the first 20 validation
batches are a selection monitor, not that scientific score.

Before any new successor dispatch, project CPU and wall independently from
observations, including probe, validation, restore and serialization. Demonstrate
hard external limits and durable interruption or implement bounded callbacks;
an after-fit check alone is insufficient. Correct "nothing fitted" if a probe
already trained. No hot patch or silent budget increase on the running child.

## 2. Local evidence lane: identity and recovery first

Freeze the review's PRE cases as behavioral tests, then repair the public APIs:

- Resume validates task/code/model/data/calibration identity and recomputed
  manifest digest, profile and accepted delivery linkage. A changed identity
  requires a new run. Local history never gains governed authority by resume.
- Reconstruct open attempts from durable events. Validate start/finish/retry
  transitions, foreign attempts and contradictory repeated finishes. Retransmit
  one event idempotently; retry computation uses a NEW attempt linked to its
  predecessor. Distinguish those operations rather than deduplicating by value.
- Give metric events stable identities and validate finite numeric values (not
  bool), status/value consistency, population identity/count and all unit/scale/
  definition fields. Preserve horizon, target, split and attempt grain.
- Recover only an identified torn trailing append. Preserve/quarantine corrupt
  bytes and reject interior corruption; no silent filtering in the OLAP loader.
  Test interrupted append/manifest replacement and competing writers or enforce
  and test an explicit single-writer contract. Bound paths to the run root.
- DuckDB stays optional and reconstructible. Preserve metric grain in views,
  avoid double-counting overlapping sample populations, and publish rebuilds
  atomically. Declare the installable optional dependency and execute its tests
  in an isolated environment; no production cube changes.

## 3. Runtime lane: typed outputs and binding, then actual plugins

Validate operation-specific requests BEFORE load/infer. Validate payloads against
the declared task/output types, including null, nonfinite values, axes/horizons,
probability/quantile constraints and explicit abstention. Unknown or unimplemented
schemas refuse; `schema_valid` cannot be a synonym for status OK from a provider.
Reuse the existing classification validators; do not invent a second divergent
probability contract.

Calibration binds actual model state, task/output semantics, eligible population
and clocks/as-of. Foreign, future, missing and incompatible calibration records
must not set `calibration_bound`. Provider exceptions must retain their real
class, not all become resource exhaustion. Exercise wrong-state and malformed
requests through the actual dispatcher.

Define and test supported entry-point forms (class/factory/instance as intended).
Install a tiny fixture distribution and exercise discovery through metadata,
then one actual available engine through the same public path. A recording double
proves dispatch behavior, not real-engine integration. Keep unsupported families
explicitly unsupported until their real adapter round trips are verified.

## 4. Governed and DOIN lane, opt-in and independent

Continue INT02/04 and INT06-10 already assigned. Use the real data-gov adapter's
accepted-delivery semantics, not truthiness of a returned dict. Demonstrate one
local run and one governed run through the same task code; denial before work,
receipt identity, actual terminal/outbox and reconciliation. No local fallback.
Use disposable services for failure injection, not outages of live hosts.

Exercise the actual doin-core plugin loader/evaluator round trip with a bounded
candidate: immutable objective/direction, finite scalar fitness, full metric
records retained separately and retry idempotence. Governance and DOIN remain
independent optional choices; no required server stack for community examples.
Do not optimize away temporal/holdout/causal/risk constraints.

## 5. Integration and delivery

Merge current M5PHET main's approved integration design additively (candidate
shares base 4508b98, main fd437d6 contains newer docs). Preserve concurrent work.
Update method state and requirement-to-evidence matrix to distinguish implemented,
integrated and independently accepted. Keep existing news-signal pins until its
real consumer tests pass against the candidate; no placeholder implementation.

Parallelize bounded code lanes with available Hermes agents in disjoint worktrees
and a single integrator. Continue useful admitted experiments with external5090
first; do not allocate pointless GPU jobs for utilization alone or preempt running
work. Continue eligible calendar/feed/paper-adapter work without waiting for these
code reviews. No real-capital orders, no final-reserve access, no submission.

One return after independently executable assigned work, not permission requests
per step: actual experimental result or running job, per-delivery status, PRE/POST,
real entrypoint/engine/consumer evidence, tested dependency environments, failures,
remaining exact missing objects and aggregate measured costs. Missing evidence is
not a positive result. These repairs must not relabel historical metrics or launch
blanket retraining.
