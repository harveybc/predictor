# Satoshi: RP132-RP135, resume reference experiments; isolate retention repairs

Reviewed base `c9dce617`; read the [independent review](../audits/work_plan/MUSASHI_RP128_RP131_REVIEW_2026_09_23.md)
and preserve its probe/results. Supersedes RP128-RP131. One return after all
feasible work; no further engineering authorization needed. The owner's latest
instruction requires experimental progress, not another repair-only round.
Execute RP135's existing reference campaign independently of RP132-RP134.
Start its preflight/design first; do not wait for a new review of retention code.
CPU repairs must not compete with training on the same thermally limited host.
Apply the [concurrent execution policy](../tres_temas_entrevista/program_v3/CONCURRENT_EXECUTION_2026_09_23.md)
and its persistent twelve-cell queue. Assign available Hermes/subagents to
experiment execution, isolated repair and analysis/design roles; preserve their
other assignments. Use separate worktrees, pinned training code and one GPU
dispatcher. Do not make staffing or a new scheduler a prerequisite: the existing
durable runner allows training while Satoshi edits another tree. Start the
admitted science lane before spending the round on repair suites.

Keep the standing 14,400 CPU-second ceiling; record CPU, GPU elapsed time and
wall separately. Limit retention repairs/verification to 1,800 CPU seconds of
that total this round. At that limit preserve files, report the remaining defect
and continue independent feasible science. Do not create another global stop.
No changed scientific parameters, widened tolerances, private reserved-data
access or service maintenance. Do not repeat the eleven numerical reductions.
Physical cooling holds are unchanged; the external 5090 remains first and only
GPU eligible after fresh admission. No production prediction deletion in this
round: sufficient disk without assumed deletion is an RP135 admission condition.

Only a demonstrated defect in RP135's actual data, training, scoring, governance
or admission path can stop RP135. Name the affected operation and evidence.
An unresolved deletion certificate, old-device replay or readiness report cannot.

## RP132: certificate derivation from the complete field-level record

Freeze the supplied PRE: empty `fields` with green summaries; a DISAGREEMENT
field with green summaries. Both currently certify and delete through the real
API. Declare expected acceptance tests before implementation.

Derive the required field set and tolerances from the bound estimator inventory,
then validate actual field results: presence, exact required coverage, typed
finite differences, applicable status, declared undefined behavior, tolerance,
and consistency with each family's coverage. Derive the certificate and summary
from those validated facts; do not let `families_complete`, `unchecked`,
`disagreements`, `fully_independent` or `pass` substitute for them. Reject a
contradictory record rather than silently repair its evidence.

For legacy certificates without an inventory digest, justify compatibility from
their recorded field definitions/results and producing revision. Family names
alone cannot establish the version. Reuse sufficient evidence with a dated scope;
missing evidence stays qualified rather than being retroactively manufactured.

Exercise the actual deletion consumer with faithful accepted fixture terminals
and backup, including the valid positive control. Cases: no fields; one missing
field; failed field with stale summary; unchecked family detail with green global
summary; excessive/non-finite/bool difference; altered tolerance; wrong inventory;
and interrupted/resumed deletion. Refusals must preserve every candidate file.
Keep catalog/replay separation and all previous design/type protections.

## RP133: current and unambiguous revalidation semantics

Distinguish locally recorded numerical evidence from evidence currently accepted
under its design and catalog identity. Summary acceptance must require the same
validated relationship as its detailed row, for catalog AND regeneration sources.
Keep historical inspection possible when the warehouse is unavailable, but label
it explicitly; no green accepted summary above a refused/unavailable source.

Read the actual current closure and its accepted identity before describing
current cell verification. Do not reuse an old certificate's cell flag. Preserve
the previous closure as historical evidence without calling it current.

Do not maintain a second, weaker destructive gate in the reporting function.
If revalidation is metadata-only and does not check backup/content/readers and
other deletion prerequisites, name the result as a CONDITIONAL PREVIEW, not
`deletion_eligible_today`. Actual deletion must always perform the complete gate.
Tests must cover the two supplied revalidation cases, changed/missing catalog,
unaccepted regeneration, stale closure, failed source and a valid positive case.
Ensure report/CLI failure or incomplete status cannot be mistaken for a complete
accepted reconciliation. No array read is needed merely to report metadata scope.

## RP134: retained-evidence recheck and one scoped return

Run the previous unchanged probe, the new probe and focused regression tests.
Record exact interpreter/dependencies, skips, host admission and measured cost.
Recheck all twelve retained evidence records under the repaired validator and
current accepted relationships; do not recompute model predictions. Retain the
eleven successes if their content supports them, qualify any actual deficit,
and publish a dated successor revalidation plus content reconciliation.

Keep MSE/MAE values, naive/seasonal comparisons, literature protocol and historical
reports intact. Table must separate score, numerical evidence, custody and replay;
no full benchmark claim or verified four-horizon mean until the existing gates
are genuinely satisfied. Correct overbroad RP131 wording beside the original.

H96 retains its arrays; H192 s2021 retains its historical score and incomplete
numeric certification. Original-device work remains conditional on explicit
cooling confirmation and admission, without guaranteed parity. Do not repeat
failing 5090 inference or ask the owner to approve software steps individually.
Finish this bounded software work independently of those physical holds.

Update plan/state incrementally, commit/push, synchronize workers and provide one
return. No new hardware, compression, broad refactor or expanded metric catalog.

## RP135: execute the published L512 reference, not another toy or audit

Scientific question: what normalized MSE/MAE does the author's released
long-context recipe achieve on the official ECL population, with three seeds
and matched naive controls? This establishes the long-context reference needed
before doctoral interventions. It is NOT an isolated context ablation, financial
evidence or proof that Table 9's undisclosed per-horizon lookback was 512.

Use [the existing mapping and measured pilot](../tres_temas_entrevista/program_v3/SOTA_PROTOCOL_B_MAPPING_2026_09_23.md):
TimeFilter `dffde87e`, released `scripts/ECL.sh` L512 block, full 321 channels,
patch128, all author optimizer/model/preprocessing/checkpoint defaults resolved
from that revision; horizons 96/192/336/720, seeds 2021/2022/2023: **12 cells**.
Keep 15 maximum epochs and the author's early stopping, not a shortened recipe.
If the legacy runner calls this `Lsearched`, explicitly bind its executable
lookback to 512 and label output `RELEASED_L512_RECIPE`; it is not a search.

Before fitting, seal the actual design, environment, data/scaler/split identity,
all resolved arguments, finite population, metric reductions and replay rule.
Register governed inputs and units before preparation. Reuse proven preparation
and scorer paths, with their existing tests; do not rebuild the audit framework.
Use the author's normalized float32 MSE/MAE, plus independent float64 diagnostics
separately, matched persistence and daily seasonal controls on identical targets.

Preflight the external 5090 by physical UUID and assert it inside each child;
check host/GPU thermals, memory, competing jobs and real disk. One cell at a time.
Inventory existing retention, then calculate the peak bytes of all twelve cells
from actual shapes/dtypes, including targets, replays, catalogs/checkpoints and
temporary copies. Keep the existing 50 GiB filesystem reserve; no promised
deletions, new disk, compression or full-array copies to the traveling laptop.

Use the already measured cost pilot: about four GPU wall hours for training
across twelve cells, NOT a full-round bound. Explicitly add scoring, replay,
catalog and validation costs, and check the separate CPU ceiling. Stage cells
in predeclared horizon/seed order when needed; finish every admitted cell's
original training budget rather than reducing updates or selecting by score.
If the complete population cannot fit the existing allocation, complete the
predeclared feasible prefix and state the measured remaining capacity need.
No request for routine permission between cells. Never exceed a hard limit.

Replay each cell on the SAME external GPU immediately after evaluation while
its arrays exist, under the frozen rule. Recompute targets/metrics and bind
artifacts to accepted terminals; reconcile content with the warehouse. Preserve
arrays while retention repair is pending. No historical A replay is needed to
execute B, and no A cell is substituted into B's population or pooled mean.

Every progress/audit return leads with new experimental work and active queue
state, not software test counts. Explicitly distinguish RUNNING, measured and
accepted, and report the actual reason for any GPU idle interval. Prepare the
next master-plan design before the queue empties, without changing a running
design or claiming that its scientific dependencies are already satisfied.

Return actual per-horizon seed scores, mean/SD, naive, source and protocol
comparability, allocation/observed updates, stopping cause and measured cost.
Table 9 scores remain PUBLISHED_WITH_UNRESOLVED_LOOKBACK, not an exact matched
comparator; executing its released script does not resolve that missing fact.
Compare A/B on common test identities only where each has adequate evidence;
label the contrast as a multi-parameter RECIPE comparison, not a context effect.
Do not issue an unverified four-horizon A mean. No tuning on either test score.

On completion, map the accepted reference to the next existing doctoral
intervention (extractor/pretraining regime under the modular plan), specifying
the components held fixed and prerequisites. Do not launch an improvised ablation
or choose a financial/RL architecture from these electricity results. A B result
is advancement of SOTA-REPRO, not confirmation of the doctoral hypotheses.
