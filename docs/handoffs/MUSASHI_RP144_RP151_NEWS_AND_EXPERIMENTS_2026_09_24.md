# RP144-RP151: continue experiments and implement the independent news track

Based on Satoshi `07140d03`, RP140-RP143 return, plus the owner's approved Laya
track and subsequent coordinator-hosting question. All eight orders form ONE
assignment. Execute independent work concurrently; no routine continue prompts.
These are implementation/demo-paper orders, not real-capital authorization.
Owner addendum: **M5PHET is a typed ML framework**, not a research-program label.
Use https://github.com/harveybc/M5PHET and news-signal's pinned runtime dependency;
Its classification contract is implemented; the general provider runtime and
four other task families are designed, not shipped. Calendar-first use cases and
P01-P09/CAL01-CAL12 tests are specified in that repository. Apply the expanded
RP150 to the next independent implementation increment, without restarting work.
Do not block the existing orders to build all five. See the expanded RP150 below.
Read [the scoped independent review](../audits/work_plan/MUSASHI_RP140_RP143_REVIEW_2026_09_24.md)
and freeze its probes unchanged before corrections.

## RP144 - qualify and check protocol A without training again

Preserve the reported four-horizon MSE/MAE 0.161962/0.259662 versus published
0.158250/0.255750. Scoped code review and CPU counterprobes were performed, not a
new original-array/warehouse audit. Keep device attribution UNKNOWN or
INFERRED exactly as measured; bit-identical replay does not recover training UUID.

Attack the composition's `METRIC_AND_SHAPE_BOUND` branch before using it as strong
identity: a wrong population/checkpoint with equal scalar metric and shape must
not acquire artifact-bound verification. Bind original report/terminal/checkpoint/
input population, preserve weaker evidence as qualified, and test omission,
duplicate horizon/seed, foreign accepted terminal and stale local registry through
the real closing entrypoint. Reuse already accepted identity-bound evidence;
repair only the affected closure, never retrain to manufacture provenance.

Executed findings: the metric-only branch checks no shape and still pools;
report rows lacking all accepted-artifact identities bind; empty binding sets
with row_verified=true bind; a claimed checkpoint without a root counterpart
becomes IDENTITY_BOUND. Require actual present comparisons, do not trust equal
flags or the absence of a mismatch. The retained composition uses four report
replays and eight identity histories, not the metric-only branch: the attack
does not prove the reported mean wrong. Revalidate that population's real links
and publish an additive corrected closure without invented device provenance.

## RP145 - finish B through its existing executor

Read live service, real flock, child UUID, remaining cells, outbox and cost ledger.
The last return reported ten of twelve accepted training terminals and H720s2022
running. That snapshot is not current authority. Do not start a duplicate.
Complete pending cells/replays/reductions on the external5090 under the unchanged
24000CPU/8h continuation allocation and current memory admission. If allocation
is exhausted, preserve unfinished state and finish independent assigned work;
do not relabel a fresh budget as past permission.

Close twelve cells by accepted content, official float32 MSE/MAE, same-row naive,
seed dispersion, cost and exact execution recipe. Table9 lookback ambiguity stays
explicit; B is the released L512 recipe, not claimed exact Table9 identity. No
test-driven recipe changes, no blanket prediction deletion/compression.

## RP146 - move the doctoral experiment forward

Review the new `df_ecl_modular` graph and input support, not only its shape tests.
Require actual detector/fusion/receiver dimensions, per-channel output ordering,
effective temporal support, target support, scaler population, fixed AE masks,
purged inner-TRAIN validation, restored checkpoint and real optimizer update counts.
Independent target calculation must not merely call the same author dataset twice.
The present check does exactly that and bypasses delivered `_TrainWindows` targets.
Test actual batches against an independent reference oracle at split boundaries,
all horizons and channel order, and independently recompute metric reduction.
Freeze AE validation masks by stable window/channel identity: the same validation
batch currently changes20 mask positions on repeated reads in the CPU probe.
Remove `reload_parity ... or d1 == d2`, which accepts the same wrong donor twice.
Require equality to saved AE-source bytes, fresh-process prediction parity and
an observed optimizer step showing R1 unchanged and R2 changed. Gradient existence
alone is not a measured update. Preserve the pilot as cost/path evidence; its
changing-mask validation curve does not measure a fixed reconstruction task.
R1/R2 use the same donor bytes; R1 detector frozen, R2 updated; all other factors
held constant. Persist AE cost and equal-total-cost interpretation.

Once those checks and the actually consumed protocol-A reference pass, seal and
run the first **development** R0/R1/R2 contrast at H96, three paired seeds, full
321-channel target and exact A split/normalization/reduction. Use the approved
modular architecture, not a smaller proxy. Readout generalization must be explicit.
Freeze fit/checkpoint/patience and budget using the measured TRAIN-only pilot;
do not select a recipe from public-test scores. Outer validation may be used only
as declared; no private reserve or confirmation claims. Choose the exact sample
and update budget before scoring, report cap censoring and training curves.
Stage this finite contrast under a new 14400CPU-second / 8h-wall allocation,
including donor, scoring and validation; cost-pilot first, no silent removal of
cells if it does not fit. WORKER_A can run while B owns external5090, or use the
5090 next when free. News software repairs are NOT a dependency.

## RP147 - real Laya installation and coordinator pilot

Use new [news-signal](https://github.com/harveybc/news-signal), read its README,
AGENTS, method state and tests. Current evidence is fixture/SDK-double only.
Install in an isolated environment; pin the specified SDK commit and full Hub
checkpoint revision, license, encoder/tokenizer/config/weights and all numerical
dependencies. Start English relevance/event/tone only, not an invented return model.

First candidate for resident news inference is the coordinator RTX4070 8GiB,
after fresh admission. At inspection it had6.3GiB free VRAM,19GiB available host
RAM and46C, no compute job. Those figures are not a completed Laya fit test.
Real smoke budget600CPU seconds /900wall seconds after package/download setup;
one checkpoint, batch1,2CPU threads, no compile, no fast kernels and no Router
preload. Freeze a short synthetic/domain-labelled probe before running. Record
load and peak memory, p50/p95 latency, input lengths, UUID observed in child,
SDK/checkpoint identity, truncation refusal, no-network inference, failure paths.
If it does not fit, use an admitted independent worker or queue external5090;
never evict the scientific job. Favor the5090 for later substantial fine-tuning.
Do not treat its confidence or act_probability as calibrated trading probability.

## RP148 - start the prospective news stream and shadow service

Use existing licensed account/feed entitlements; inspect only their documented
service configuration references, never enumerate unrelated secret stores.
Implement first-receipt/event/publication/revision times, original content hash,
language and externally verified symbol mapping. No publication-time backdating.
Persist dedup/revision state across restarts, bounded queues, refusal/lag counters
and source outages. Never turn missing news into neutral sentiment.
Govern inputs/model outputs through data-gov before reporting scientific runs;
local news-signal hashes alone are explicitly NOT accepted governance evidence.

Deploy a bounded resident classifier once the real smoke passes. Shadow has NO
broker mutations. Use separate namespaces and profiles from existing trading.
Prospective collection can continue while calibration/policy work proceeds.
Seal a small task-specific annotation/calibration design and matched baselines;
FinBERT only as a like-task comparator, not copied unrelated published accuracy.
Language beyond English is separate checkpoint/task acceptance, not auto-routing.

## RP149 - connect MT5 demo AND Alpaca paper through existing risk

Implement the versioned feature-to-policy interface outside news-signal. Preserve
ordered features, availability, input/model/config identity, abstention and expiry.
An uncalibrated tone label is not a buy/sell policy. Freeze a transparent research
policy and its risk constraints before the paper experiment, then carry its
`AssetIntent` through `DemoExecutionService.process_intent`.

Reuse Alpaca `AlpacaL1Executor.consume_pending` with fixed paper endpoint and
MT5's LTS risk-accepted order -> `Mt5ExecutionStore` -> signed MQL5 bridge.
Do not bypass by directly invoking a transport client or queue method. Existing
no-op generic broker classes are not the implemented execution routes.
Separate these account checks: Alpaca ACTIVE/trading_blocked/account fingerprint,
buying power and shorting permission; MT5 DEMO, account identity, netting/hedging,
symbol trade mode, market session, ownership/magic and stop distance. Start
long/flat; enforce close-to-flat without accidental short. Require signed route
identity, not a legacy anonymous bridge. Reuse the existing approved paper/demo
mandate; do not invent owner capital/exposure settings.

Test accepted risk intent -> protected order -> real acknowledgement/fill ->
reconciliation, then repeat/restart/reconnect with no duplicate economic effect.
Include stale news/quote, partial fill, rejection, uncertain timeout, competing
position, expired signal and kill/hold. MT5 and Alpaca are independent deliveries.
Canaries use isolated paper/demo profiles, never the active strategy's positions.
`tick(allow_execution=False)` is NOT a guaranteed shadow path in existing Alpaca
code: drain/monitor actions precede that flag. Test shadow with ZeroNetworkSink.
No real-money activation is authorized by these orders.

## RP150 - financial prerequisites and M5PHET product integration, in parallel

Keep FIN-LOSS-OPT mandatory. Resolve the financial producer's timestamp/receipt/
completion contract from evidence. UNKNOWN stays UNKNOWN; do not fabricate a lag
to bypass the422 or claim archive-only data is point-in-time. If that resource
cannot support the task, name the exact evidence/source successor needed. A
prospectively timestamped news route need not wait for this historical FX resource.
Prepare the matched financial reference independently without reading the reserve.

The typed-ML framework design now lives in https://github.com/harveybc/M5PHET:
classification, regression/forecasting, representation/unsupervised, RL and causal
are five task families; DOIN optimization is cross-cutting. news-signal is
the first real consumer of its classification contract. Verify the pinned
dependency and the actual nested typed_result, not just a README link. Preserve
no-order authority, explicit uncalibrated status and data/model/task identities.
For each next family, map the existing domain repo, task-specific output and
uncertainty semantics, current open-source reference and matched acceptance
protocol before adding an engine. Forecasting must cover direct-horizon versus
joint multi-output as a designed contrast, not a default inferred from Laya's
ordinal score. Causal effects require identification assumptions and refutation;
RL requires environment/reward/policy evaluation, not a relabelled classifier.
Select the best suitable open-source engine by measured task evidence, license,
resource fit and deployment needs. Do not present a fixed universal champion.
The directory submission links BOTH repos: news-signal as source URL, M5PHET
and news-signal URLs in Description. Name optional field blank; no personal
biography in public text and no promise that an existing GitHub account is
anonymous. No claim upstream will implement/fund our project. No external
submission by Satoshi. This addendum does not invalidate completed RP144-RP151
work or authorize restarting existing experiments.
Keep the future feature-branch vs final-head ablations explicit, not assumptions
that Laya replaces predictor or that an unmeasured model improves live trading.

Product clarification, 24-sep UTC (M5PHET design revision `4508b98`;
runtime dependency remains unchanged): read docs/USE_CASES.md, INTERFACES.md,
ECONOMIC_CALENDAR.md and IMPLEMENTATION_PLAN.md. State + task/schema -> typed
result is the intended interface; reject unsupported capabilities before work.
Reuse Laya's SDK for choice/binary/ordinal tasks, with explicit semantics and
negative tests; recommend direct SDK use where no composition is required.
Do not invent five model implementations to fill the five interfaces.

First independent increment: inventory the EXISTING economic dataset and map
source/resource/units/field clocks, consensus and actual/revision vintages, rights
and gaps. Implement the vintage-aware as-of transform and CAL01-CAL12 negative
tests in the appropriate data/feature boundary before consuming it in ML. Missing
historical availability constrains that experiment only; preserve UNKNOWN and
start prospective capture where entitled. No guessed zero latency or invented
source registration. Numeric release values come from source records, not Laya.

Specify/wire consumers to existing engines: hierarchical market state with an
inductive assignment rule; predictor multi-horizon output with declared Bayesian/
quantile/distribution method and calibration; actual RL observation/action path.
Calendar-only RL need not await a forecasting winner. Seal price-only vs known
schedule vs observed-surprise ablations on the SAME origins, model and budget;
separate capacity and representation changes. Causal effects require a distinct
identified study with pre-treatment support and sensitivity, never sentiment
renamed as causality. Geopolitical text follows structured calendar ingestion.

P01-P09 are acceptance designs, not passing model tests. Work within existing
lane budgets; continue current admitted experiments in parallel. Report actual
software/data progress without claiming these new workflows trained or validated.

## RP151 - one return, evidence first, all lanes accounted

Delegate to Hermes/subagents where useful: scientific executor/analysis, news
collector/model, broker integration. Disjoint worktrees, single real lease per
runtime; agents receive no secrets in prompts. Do not unblock the expired RP135
Hermes task. Complete all independent assigned work before returning; only actual
missing entitlement/account mandate or violated measured budget blocks that lane.

Return tables: experimental error/metric/same-row naive/literature/comparability;
news classification baseline/coverage/calibration/latency/model identity;
MT5 demo and Alpaca paper status separately (orders acknowledged, fills, rejection,
reconciliation, no fake PnL); running child ownership and CPU/GPU/disk budget.
Label NO_NEW_MEASUREMENT where appropriate. Publish tests, commits, exact open
objects and next runnable item. Do not call fixture software tests financial
progress or paper gains live gains. Update program state and queue without
erasing historical evidence. No owner action is presently needed for coding or
the bounded model pilot; real broker entitlements remain to be verified.
