# Satoshi: RP128-RP131, enforce the checks at the destructive boundary

Reviewed base `a73e55b2`. Read the [RP127 review](../audits/work_plan/MUSASHI_RP122_RP127_REVIEW_2026_09_23.md)
and freeze its executable probe/output before changes. These orders supersede
RP122-RP127. Complete the feasible blocks without per-step approval requests;
one final return. Existing owner authorization suffices for the software work.

Preserve measured scores, the fourteen-family numerical implementation and all
existing evidence. No benchmark training, replacement seeds, tolerance changes,
B factorial, financial fits, service restarts or reserved-data access. No blanket
recalculation of eleven already checked cells. Keep the standing 14,400 CPU-second
round ceiling; report measured CPU separately from elapsed time.

Only WORKER_B's external RTX 5090 is eligible for GPU work, after the established
UUID/connection, host/GPU thermal, memory, disk and competing-work admission.
The traveling coordinator and both held internal/worker GPUs stay held until
explicit physical cooling confirmation. No fallback or date-based release.
This round's repairs and fixture tests should need bounded worker CPU, not GPU.

## RP128: acceptance scope must reach the deletion decision

PRE first: retain the supplied wrong-ACF example that the numeric reference
rejects but the no-data path accepts and then deletes through the normal API.
Separate domain/historical inspection from full independent numerical acceptance.
Missing data, disabled independent calculation, absent fields or incomplete
coverage must not produce the approval used by deletion. Preserve diagnostic
reports with an explicit restricted scope rather than discarding history.

At the destructive consumer validate the accepted certificate's exact catalog,
population, estimator inventory/version and complete successful comparison, not
only `pass`, a digest and a COMPLETED terminal. Require the actual declared field
coverage and absence of discrepancies. Do not solve this by making another
caller-declared boolean authoritative. Reuse valid previously accepted evidence
whose content establishes the required facts; no need to re-read arrays merely
to change a gate. Apply the condition during resumption as well.

Tests: normal valid deletion; absent data with arrays present; independent=False;
domain-only accepted report; one unchecked field; failed comparison with a stale
pass flag; interrupted/restarted deletion; dry-run; and positive retained-certificate
reuse. A refused case must leave every candidate array intact. Use real API/CLI
consumers and faithful accepted-chain fixtures, not mocks of the decision itself.

## RP129: make scientific-design identity mandatory where acceptance is consumed

Carry expected design/run and relevant subject/role through deletion, historical
closure and regeneration acceptance. Validate against the accepted terminal's
scientific-design field as defined by its real schema. Do not confuse a different
campaign-config hash with the scientific design, or choose whichever declaration
happens to match. Reject missing and contradictory required identity.

Reproduce both supplied cases: foreign design refuses in the helper but succeeds
through actual deletion; missing identity passes despite an explicit expected
design. Cover conflicting declarations, the right digest under another subject,
and positive production-shaped terminals. Reconcile existing accepted objects
read-only against this contract. Where historical metadata is insufficient,
retain the qualified history and publish a validated dated successor only when
the retained content justifies it; never manufacture past acceptance.

## RP130: numeric types and focused integration regressions

Reject booleans in declared numeric fields, including nested lists/dictionaries.
If a comparison genuinely supports boolean metadata, it must be explicitly
typed and never compare equal to a numeric operand. Freeze the false-as-zero
histogram example through actual catalog acceptance, plus true-as-one, null,
NaN and constant/undefined controls. Do not change metric formulas or tolerances.

Run the prior counterexamples and focused suites with positive controls. Report
exact interpreter, dependencies, skips and runtime. Test failures in a helper
are insufficient unless the real destructive/evidence consumer also refuses.

## RP131: successor metadata closure, conditional device work, one return

Inspect the eleven retained successful comparisons and accepted identities under
the repaired gates; reuse sufficient evidence rather than regenerate. Preserve
original scores and issue dated scope corrections for the old broad completion
claim. Report numeric, replay, provenance and deletion-eligibility status separately.
Query/reconcile any new accepted successor by content. Do not delete any real
array until every applicable repaired gate passes, backup bytes are verified and
the thermal/access policy permits touching that host.

H96's three original arrays remain retained while replay is unaccepted. H192
s2021 keeps its accepted historical score and explicit incomplete catalog status.
Do NOT repeat its failing inference on the 5090 just to obtain the same refusal.
If the owner confirms restored cooling during the round, fresh admission may
allow a bounded original-device replay for H96 or inference for H192 s2021.
Bind the environment/checkpoint/input identity, compare digests and frozen rules,
and stop that attempt on mismatch; no promised equality, wider tolerance or fit.
If cooling is still unconfirmed, complete the software work and list those two
physical holds once at closure. No new owner decision on scientific thresholds.

Generate the usual table from accepted evidence: official normalized MSE/MAE,
same-population persistence and seasonal baselines, paper values and exact
comparability scope. No verified four-horizon average with an unverified member.
No new measurement must be labeled as such. Update work plan/state incrementally,
commit/push, synchronize workers and submit one consolidated review request.
