# Satoshi: RP114-RP121, close acceptance boundaries before more cleanup

Reviewed base: `e145303c`. Read the [RP113 independent review](../audits/work_plan/MUSASHI_RP106_RP113_REVIEW_2026_09_23.md)
and its unchanged executable probe/results. This supersedes RP106-RP113. Complete
all feasible blocks before the single return; no per-step owner approval requests.
Update state incrementally. No benchmark retraining is authorized by this order.

Keep the external RTX 5090 as first choice and currently the ONLY GPU eligible
after UUID, connection, thermals, RAM/VRAM, disk and competing-work checks. Bound
and monitor host CPU work too. No internal-GPU fallback, automatic reboot, held
device replay or automatic release on a calendar date. Do not interrupt unrelated
work. Lightweight independent repairs do not wait for WORKER_A cooling.

## RP114: freeze five actual counterexamples before implementation

Preserve Musashi's fixture and outputs. Add actual-entry-point tests for changed
report plus rehashed marker, fabricated regeneration, deletion without approval/
backup, the N=16,777,217 float32 mean, and the deliberately broken catalog producer.
Keep valid controls. Test the finite-number/type domain, including NaN/inf and
missing evidence, not just the exact bad values used by the probe. Do not replace
the producer mutation by a manual file edit that only tests a digest mismatch.

## RP115: bind original closure AND regeneration to accepted evidence

Use the existing governed artifact/acceptance mechanism for the exact closure
and regeneration/acceptance outputs, linked to campaign, unit, design, record,
checkpoint, inputs, population, scorer implementation and metric space. Resolving
a digest named by a freely changed local marker is not independent acceptance.
Forged report plus recomputed digest must refuse even with valid unchanged input
receipts. Fabricated ACCEPTANCE.pass and copied input hashes must not replace a
score. Reject contradictions, missing/duplicate units and non-finite outputs.

Do not rewrite old records to pretend they carried a new guarantee. Inventory
which current reports have an independent prior anchor and which need a clearly
dated successor acceptance. Reuse verifiable retained evidence; regenerate only
the numerical material actually needed from checkpoints, never retrain. A trusted
input record alone does not authenticate a later closure's or regeneration's
reported output. Preserve explicit scopes for accepted historical evidence.

## RP116: mandatory deletion prerequisites, through API and CLI

Require the independently accepted report/catalog identities and a verified
durable backup, with typed refusal if any is absent. Require catalog acceptance,
not merely its optional presence in a directory. Check backup destination bytes
and the exact acceptance/backup manifest; a caller-supplied mapping of local file
hashes is not proof that the backup exists. Recheck on resumption too.

Retain whole-unit content preflight and per-path receipts. Verify that writers,
transfers and competing deletion roots cannot invalidate the claimed exclusive
boundary between identity check and unlink; root-local flock alone does not
make other processes cooperate. Test interrupted deletion, restart, changed copy,
missing/corrupt backup, changed acceptance and aliases. No new production unlink
until these conditions pass. Existing owner authorization is sufficient; what is
required here is verified evidence, not another permission request.

## RP117: independent estimator validation, not self-consistency alone

For the finite approved catalog, implement separate numerical references/oracles
for errors and baselines, moments, correlation/R2, ACF, histogram quantiles,
entropy and MI. State each formula, sample/lag support, reduction, estimator
parameters and undefined/approximate behavior. Test constant series, deterministic
sequences, clipped/out-of-range bins, empty/short support and zero denominators.
Independently compute expected values from input fixtures without calling the
catalog producer. A reference library may be used where its definition matches.

Retain consistency identities as additional tests, not certification of unrelated
families. Require finite valid domains and named undefined states; test per-family
mutations so a broken producer cannot pass by agreeing with its own summaries.
Apply independent checks to real-cell evidence where possible; label historical
limits explicitly where arrays were deleted. Do not claim all nine actual catalogs
were numerically certified merely because the file hashes remain unchanged.

## RP118: fix and version the author-float32 scorer

Reproduce the integer-denominator rounding failure against the unmodified author
operation in the pinned environment. Preserve the original NumPy accumulator,
scalar promotion and final division semantics, not float32(N) by assumption.
Extend parity coverage across the 2^24 boundary, actual benchmark cardinalities,
partial leaves and diverse magnitudes, with bounded memory and independent
unmodified-author comparisons wherever admitted. Tests must cover denominator
and sum separately as well as the whole function. Keep float64 as a separate
check. Version the route and retain its earlier claims/results as superseded.

Measure per-cell impact on actual scores before asserting any changed scientific
conclusion. Reuse retained original arrays or regenerate once by inference where
needed; compare original prediction/target digests. Preserve any mismatch, and
never widen replay tolerance or refit to obtain equal values. Respect disk and
memory envelopes and remove scratch only through corrected acceptance.

## RP119: scoped successor closure for protocol A

Close all 12 cells with separate states for measurement, custody, original scorer,
catalog acceptance, replay and device attribution. Correct T96's presentation to
measured-but-replay-unverified; its held original-device work remains conditional
on cooling confirmation. A new measured UUID cannot retrospectively change an
old inferred training attribution. Do not retrain solely for attribution.

Reverify repaired evidence against the live accepted chain and retain old reports.
Only independently anchored/computed outputs enter verified aggregate summaries.
Report exact changed/unchanged scores, artifacts and reasons; no blanket restart.
Only after RP115-RP118 and per-cell independent acceptance may corrected cleanup
resume. Inventory original/regenerated copies and measured disk space, preserve
the metadata/checkpoint budget, and keep unready units' predictions until ready.

## RP120: correct protocol-B planning without a substitute campaign

Correct the cost document: 96/24 and 512/128 both give four patches per channel;
the custom loader's test window count at fixed split/horizon is unchanged by L.
Verify those properties against the real loader/model, not comments. Preserve
the distinction between the public fixed-L512 recipe and unknown Table-9
per-horizon searched choices. No unqualified exact-Table-9 claim.

Replace the assumed 2-4x/VRAM-fit estimate by either an explicit UNMEASURED status
or an admitted, governed, bounded exact-recipe resource pilot on the external
5090, with no selection from test errors. Record actual host/GPU/disk/validation/
scoring costs and project the finite population. Do not launch the B factorial
in this order; protocol A's acceptance repairs remain the priority.

## RP121: final review package

Run the real PRE/POST probes, relevant suites and mutation tests in admitted
worker scopes; report exact interpreter/dependency/skipped-test scope. Table:
official normalized MSE/MAE, independently checked float64, matched persistence
and seasonal baselines, paper source/protocol, per-seed results, mixed-host SD
scope and verification status. No verified four-horizon average with an unverified
member; no claim that an operational margin proves statistical equivalence.
Generate the narrative SDs and table from the same source to avoid inconsistent
rounding/counts. Reconcile successors by content, publish digests and measured
resource/cleanup ledger, update work plan, commit/push and synchronize workers.
One return covering all feasible work; clearly list any real physical/evidence
limit without assigning independent engineering tasks back to the owner.
