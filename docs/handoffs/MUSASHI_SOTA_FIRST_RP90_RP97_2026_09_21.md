# Satoshi: RP90-RP97, SOTA reproduction takes priority

Base inspected: `3389a25`. Owner explicitly replaces the next experimental
priority. Read [SOTA_FIRST](../tres_temas_entrevista/program_v3/SOTA_FIRST_2026_09_21.md)
and the master/state before work. Finish all feasible dependent and independent
blocks without asking the owner to approve each step. Progress is not completion.
Do not declare reproduction complete after only preparing a design or a pilot.

## RP90: remove historical DEV from active decisions

Preserve files, receipts and failures. Implement the new HISTORICAL_DEV_ONLY
disposition in actual selection/query paths for the previous exploratory pilots;
historical dashboards remain available and visibly separate. Test that an old
low-error result cannot enter the active reference ranking, including via a
cached table or inherited closure. Do not rewrite history or delete unfavorable
scores. No more simplified-model/context/financial fits under the older orders.

## RP91: establish the strongest reference, not a convenient substitute

Review primary papers, appendices and author code for full ECL321. Record exact
dataset, horizon, context, split, scaling, metric and per-horizon published values.
Include PatchTST, iTransformer and newer stronger claims such as TimeFilter and
NPMixer; the list is not closed. Resolve protocol differences before ranking.
If MAE/MSE leaders differ, preserve the distinction. Name missing artifacts and
paper/code disagreements. Do not label a fallback the best. Do not reuse the
household task, custom 32-client panel or an adapted GRU as ECL reproduction.

## RP92: freeze the full author recipe and acceptance first

Create a source-and-protocol lock with hashes and versions, exact architecture
and every effective default, dataset provenance, preprocessing, partitions,
training, validation, metrics and numerical agreement criteria. Pin a revision
consistent with the chosen paper. Download through governance, including the
official processed benchmark, rather than rebuilding a subtly different task.
No private financial holdout. Tests must fail on different scaler, channels,
window, horizon, target, reduction, checkpoint or substituted predictions.

## RP93: audit the executed ML path and repaired evidence custody

Run the author model, loader, loss and scorer, not approximations written for
tests. Recompute input support and feature availability by row; include future
perturbation, boundary/padding and normalization-fit controls. Verify checkpoint
reload in a fresh process and independently recompute metrics from predictions.
Do not repeat the cached replay bug: hash actual checkpoint bytes before trusting
replay evidence; bind model implementation, data, environment and predictions.
The retained RP89 external probe is at
`docs/audits/evidence/RP89_MUSASHI_REVIEW_2026_09_21/` when present in this branch.
Missing or altered artifacts must make the actual closure fail, not merely a
helper. A calibration/fit requirement is not satisfied by a manifest alone.

## RP94: provision the recipe, do not shrink it

Measure hardware capability, memory and compute using an isolated bounded
preflight. Produce a full allocation for all required horizons/seeds. The old
14,400-second pilot cap does not justify replacing the author's architecture or
truncating its dataset. This order does not authorize unlimited spending or
interrupting live workloads. Use available authorized resources and record any
specific capacity deficit; complete all preparation instead of requesting a
generic owner permission. No service changes without the existing tested
adoption/rollback procedure. Never bypass an execution-environment refusal.

## RP95: execute the faithful governed reproduction

When the source, protocol, acceptance and resource preflights pass, execute the
full selected recipe, with original loss/optimizer even if our electricity
pilot favored another pair. Do not replace Adam with AdamW, MSE with MAE/Huber,
or change early stopping while calling it the same experiment. Record any
necessary operational patch and verify its lack of mathematical effect.
Register before reading/fitting; report every terminal and actual compute.
Preserve failed attempts, close refused campaigns, and reconcile through the
live accounting/warehouse path. This is not yet a financial or RL result.

## RP96: numerical reproduction, not just equal file hashes

Independently re-read official targets, predictions and checkpoints. Recompute
the paper metrics in the paper's space and aggregation, and naive on identical
origins. Check full horizon/channel/seed populations and content-level receipts.
Report protocol fidelity separately from numerical agreement. If agreement
fails, diagnose implementation, data and environment against the frozen recipe;
do not tune on test to manufacture it. No substituted easier task or partial
successful subset may be presented as the requested full replication.

## RP97: one return and downstream plan

Return table: dataset/protocol, model/revision, horizon, published metric/value,
replicated value, difference, matched naive, seed dispersion, training completion,
cost and agreement status. Put normalized official metrics first. State exactly
what remains unexecuted; no generic SOTA claim. Update persistent state after
each block, publish changes and leave a single review request at completion.
Only after reproduction acceptance plan modular R0/R1/R2 and H-CORE against the
strong reference. Keep financial Huber/MAE x Adam/AdamW and weekly RL mandatory,
but do not transfer an ECL ranking into a trading recommendation.
