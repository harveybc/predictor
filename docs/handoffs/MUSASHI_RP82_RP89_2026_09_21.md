# Satoshi: RP82-RP89

Base: `bd6fcf4`. First read and adopt the
[RP74-RP81 review](../audits/work_plan/MUSASHI_RP74_RP81_REVIEW_2026_09_21.md)
and its executable evidence. This is the active continuation, not a replacement
research program. No further owner approval is needed for the work below.

## Execution contract

- Finish every independent block; progress updates are not handoffs. One final
  return names completed, rejected and unexecuted work accurately. An external
  denial is reported with the exact operation; do not try to circumvent it.
- Requirements and behavioral/structural acceptance first, frozen PRE before
  code changes, bottom-up implementation, then actual entry-point acceptance.
  Record progress and evidence in PROJECT_METHOD_STATE.json after each block.
- Aggregate ceiling **14,400 CPU seconds**, including all hosts, tests, failed
  attempts, pilots and verification. Reserve **2,000** for closure. Use existing
  governed workers, actor identities and memory limits; measure worker CPU.
  Fit-dependent work requires a frozen design and measured cost. Use CPU only.
- No final holdout access, live orders, scientific authorization records,
  production service restart, history deletion or blind rerun of whole campaigns.
  Atomic additive reporting and normal governed runs are in scope. Test faults
  only on disposable artifacts/stacks. Never change a live data checkout.
- Retain original returns and scopes. Correct claims beside them. Do not widen
  tolerances, reduce cells or adjust acceptance after seeing results.

## RP82: close the actual output and normalization paths

Freeze the copied-perfect-prediction and tenfold-sigma counterexamples. Make the
block closure, financial closure and table consume the same authoritative
verification result; remove their ability to certify a weaker path. Preserve
typed diagnostics rather than swallowing refusal. A failed closure emits no
verified comparator, selected winner or scientific proposal.

Bind prepared data, feature/target transformations, fit population, scaler
parameters and evaluation identities to accepted immutable preparation evidence.
Recompute the metric with that scale, not a free local file. Check native and
normalized metrics against the same task and denominator. Test changed sigma,
mean, target transform, sample IDs, horizons and old/new preparation substitution.
An integrity hash newly computed from the changed file is not an independent
anchor. Missing historic anchors yield an explicit weaker scope, not invented
custody. Reverify retained measurements without fitting; reconcile additive
successor evidence through the live service where required.

## RP83: bind comparator identity

Freeze the UNTRAINED_REFERENCE probe. Recompute canonical design identity and
bind it to the accepted campaign/configuration and exact expected population.
Verify per-cell arm, seed, role, task and reference configuration, including when
an attacker consistently rehashes a relabeled design. Reject stale digest,
duplicate, missing seed, different run and different benchmark task in the real
reference resolver. Exercise publication and consumer call sites, not a test-only
validator. Keep published literature and our adapted re-execution separate.

## RP84: execute the financial factorial as declared

The v3 registry is not accepted as proof of runtime behavior. Build v4 and repair
`select()` so A reports four fixed defaults; B tunes the same three LRs separately
within EACH loss x optimizer combination using paired validation seeds; C and D
are explicitly paired decay/delta sensitivity contrasts, not extra competitors
silently added to B. Freeze any sequential dependency and donor identity before
the corresponding measurement. No test-based hyperparameter selection.

Acceptance runs the real selector with all 26 candidates and distinguishable
fabricated scores. Force an A or D arm to beat B and prove it cannot enter B's
search. Reject duplicate/foreign records, incomplete paired seeds, non-finite
scores, contradictory identity and failed custody. Report candidate counts and
optimizer strata actually consumed, not merely declared in JSON.

## RP85: match uncertainty to the measured population

Freeze the isolated-week counterexample. Define the estimand and usable week
population explicitly. An observed week cannot count toward the estimate and
have zero chance of entering any resample without a named support limitation.
Return descriptive results and INSUFFICIENT_SUPPORT when justified; do not
manufacture a degenerate interval or silently discard an inconvenient week.

Exercise the block length, missingness rule, folds and selector call actually
used in v4. Predeclare coverage assessment, Monte Carlo uncertainty and acceptance
against the claimed confidence level. A permissive 80% assertion is not proof of
95% coverage. If defensible support is unavailable, keep descriptive paired
effects and no confirmatory interval; this need not block code/cost diagnostics.

## RP86: preserve the actual ML scope

Adopt Musashi's 15 independent fresh-process replays; do not repeat them merely
to create another receipt. Integrate an equivalent read-bound checkpoint replay
into future closure. New replay is required only for newly measured or changed
artifacts. Preserve original absolute-plus-relative tolerance and numeric scope.

Correct cross-tier interpretation using the saved training-prefix comparison.
Seed 1 has common prefixes; worker seeds do not. Record initialization, actual
environment/thread settings, batch order, update count, checkpoint selection,
validation cadence and stopping triggers. Do not label host+seed SD as purely
optimization noise, or infer a hardware cause without isolation. Keep within-seed
host blocking for new paired contrasts. No need to retrain fifteen cells to make
the old tiers look perfectly comparable. Budget censoring remains visible.

## RP87: conditional, inexpensive context experiment

Question: does a causal daily-lag channel add predictive information to the
current W60 receiver, without paying for W1440 or changing receiver depth?
Use the existing Q2 daily-lag specification and exact-crop control, tier-2 recipe,
same architecture, same three paired seed/host blocks, fixed train/DEV task and
common denominator. Explicitly freeze what timestamp the lag refers to and show
its availability at every decision; no centered interpolation or future fill.

At most nine cells: baseline, daily-lag and exact-crop control x three seeds.
Enumerate common training AND evaluation populations first. Reuse a retained
baseline only if those populations, recipe, implementation and inputs really
match; otherwise it is a new required cell. Exact-crop can inherit measurement
only with proved behavioral equivalence and declared inheritance, not shape alone.
Parameter count/capacity changes must be declared; scope the conclusion as this
feature addition, not an abstract information-only effect. A matched-capacity
negative control can replace a redundant crop fit only in the premeasurement
amendment, never after scores. Preserve all Q2 unexecuted long-context questions.

After RP82/83 acceptance, seal the cells and cost pilot. Execute only if the whole
remaining stage including closure fits. Do not lower the ceiling of individual
fits or trim the population after seeing a projection. All outputs go through
governance, common custody, per-cell inference replay and live reconciliation.

## RP88: financial v4 and a train-only cost pilot

Huber vs MAE and AdamW vs Adam stays mandatory in the actual financial domain.
No electricity winner transfers by assumption. Preserve equal tuning budgets,
train-only causal residual scaling of Huber delta, explicit weight-decay groups,
common validation monitor and comparable paired errors against naive at h6/h72.
Keep a matched literature-derived comparator requirement before scientific
ranking; use primary-source configurations and document any adaptation.

Correct the actual runner/design contract for elapsed-time horizons, feature and
label availability at each fit boundary, weekends, missing bars, scalers and
train/validation purges. Never promote a retrospective archive to point-in-time
or weekly live evidence. No rows of the final reserved test may be read.

After RP82-RP85 acceptance and verified governed temporal scope, a **train-only
cost diagnostic** is authorized, capped at 2,000 CPU seconds within the aggregate
budget. Before reading data, seal the exact pre-DEV training slice, architecture,
horizons and default recipes; use an internal purged training-validation slice
only where required to time validation. Budget up to 16 configurations (two
receivers x two horizons x four defaults), at most 200 observed updates each.
This short run estimates cost/memory, not model adequacy, convergence or a winner.
Measure warm-up, updates, validation, setup and samples separately. If the pilot
projection itself exceeds the cap, publish the measurement and full-stage cost,
not an arbitrarily shortened scientific comparison.

Derive the subsequent scientific allocation from usable examples, history length
(including the declared 52/104/208-week alternatives), batch exposure, receptive
field and learning curves. A patience of 300 updates must not masquerade as
adequate training simply because the dataset is large. Publish all 26-population
and fold counts with their measured cost. **No full financial search or scientific
test scoring is authorized in this round**; v4 and its supported allocation return
for review. Missing data rights/availability remain scoped evidence deficits,
not a demand that the owner invent factual metadata.

## RP89: complete the handoff

Return PRE/POST for the actual counterexamples, focal and full-suite commands and
environment, explicit skips, per-host CPU including verification, and content
reconciliation. Give a normalized table with task/horizon/split, error, naive,
skill, seed/host dispersion, censoring, reference value/source and comparability.
When no new measurement occurred say so. Keep kW/USD as a separate supporting
view, never mix them with MAE_z. Preserve marginal financial improvements with
full numeric precision, but do not turn rounding or backend drift into an edge.

Update the master plan and persistent state, commit and push the completed work,
make orders/results accessible to workers without changing active data roots,
and issue one return. No independent task should wait merely because another
conditional campaign could not execute. No further owner decision is required.
