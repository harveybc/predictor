# Satoshi RP74-RP81: verified comparisons and ML-sensitive continuation

Base: `1880caa`, now published. Incorporate this order and Musashi's RP66-RP73 review first.
Owner authorization is already recorded. Complete every independent block without asking to
continue after each step. One final return; concise progress updates are welcome. No service
restart, live trading, final reserve access, historical deletion or blanket retraining is authorized.
Do not describe a failed command as missing owner permission. Record its actual outcome and
continue independent work. Push and worker synchronization are authorized and now demonstrably work.

## Scope and sequence

Fix A1-A6, preserve/reverify retained measurements, then the bounded household DEV sensitivity
below. Prepare financial scientific execution correctly, but do not start a financial campaign
until its exact task/design/comparator and cost envelope have been reviewed. This is not authority
to relabel archive time as observed availability. No search for the lost historical OLAP and no
heuristic-strategy rerun prerequisite. Keep all doctoral lanes, including weekly RL and core
pretraining, in the master plan; this round is an identified ML dependency, not a new research program.

Total CPU ceiling: **14,400 seconds across all hosts**, tests/pilots/failed attempts/verification
included; reserve **2,000** for closure. Use live memory admission and isolated limits. No GPU needed.
At a cost gate, finish all independent repair/design/closure blocks instead of asking the owner
for another permission. Never shrink a scientific population after seeing its scores to fit budget.

## RP74 - Reproduce first; close the actual custody and comparator gaps

Freeze Musashi's new probes on the base before editing. Tests are acceptance requirements first,
then component/integration rules, then fixes. A local record hash is not an accepted artifact hash.
Walk accepted canonical payload -> artifact/record chain -> bytes actually read -> independently
derived metric. Reject the rewritten-array-plus-rewritten-record counterexample, missing terminal,
missing record anchor, partial reference population and prepare-only comparator. Require exact
reference model/role/task/population, not just any COMPLETED unit. Cover the table CLI and both
block/finance closures; no side verifier that the callers can bypass. Declared comparability must
be derived from the prepared task, not accepted merely because someone re-digested its declaration.

For older evidence without independent artifact anchors, preserve it with explicit weaker scope;
recover binding only from a preexisting independent canonical record. Do not issue a new digest
today and call it proof of historical custody. Publish a successor table without refitting. Apply
the strict path to new results; do not block an intact new artifact chain because unrelated older
history cannot gain a stronger guarantee. Repeat warehouse reconciliation by content, read-only.

## RP75 - Make the financial population match the consumed tensors

Through `prepare`, `PairBatches` and the training entry point, exercise one NaN/inf in every input
role, missing labels, duplicate/missing/offset-aware timestamps, split boundaries and stale bytes.
Finite scaler rows are insufficient: every consumed feature window and mapped label must meet
the declared missingness policy. Derive one shared admissible population before comparing losses,
and derive residual-scale candidates from that same admissible TRAIN population. Reject or exclude
with counts/reasons; no implicit zeros. Empty/insufficient folds do not produce scores.

Preserve elapsed-time 6h/72h mapping and explicit weekend exclusions. Distinguish W retained bars
from W elapsed hours. Bind timestamp interpretation and decision availability to the delivered
producer contract; do not discard an offset or assign completion from the label without proof.
Archive-only evidence remains archive-only; no live/PIT inference from a hardcoded availability
sentence. Test label-versus-available time and a revision/late arrival. Use synthetic fixtures and
governed contract metadata now, no real financial scientific measurement.

## RP76 - Financial selection, uncertainty and tiny-effect resolution

Do not optimize away replicate seeds. Declare selection within seed and paired reporting across
seeds, or a configuration-level validation aggregate followed by retained paired replicates; choose
one explicitly before running. Keep optimizer and loss factors identifiable. The current MAE grid
allocates 3 Adam/9 AdamW fits while Huber allocates 6/6: equal totals per loss alone do not establish
a fair optimizer comparison. Separate fixed-default 2x2 contrasts from equal-budget tuning.

Bind selection/intervals to verified complete fold/candidate/seed identities. Missing folds retain
their calendar positions. The [-0.1,+0.3], block-length-two probe must not yield a supported [0.1,0.1]
95% interval. Declare minimum resampling support and validate coverage on dependent synthetic
null/positive controls; insufficient support remains descriptive. A block method is not self-validating.
Estimate no uncertainty by treating overlapping minute windows or seeds as independent weeks.

Keep full-precision MAE_z and naive on identical rows, plus paired skill and physical units as
support. Test planted 1e-5/1e-6 changes through prediction, transformation/inversion, serialization,
terminal, warehouse and paired analysis. Separate representation/measurement floor from stochastic
training variation and sampling uncertainty. `np.spacing` alone proves neither. Small effects must
not be rounded away or dismissed, but a synthetic epsilon test is not proof of financial profit.

## RP77 - Stopping and resource accounting

Make reaching max_updates unambiguously visible, even when patience expires simultaneously. Preserve
both reasons; scan old event histories and correct affected metadata without retraining. Do not
claim convergence from early stopping or from a best checkpoint away from the final iteration.

Separate train-update CPU, all validation CPU, final restoration/replay, setup and wall time. Freeze
the double-count probe, recalculate old projections, preserve old receipts. Q2 remains budget-limited
on retained evidence; do not claim this arithmetic repair makes it fit. Profile tensor gathering,
forward/backward and validation before proposing an optimization. In particular, do not prescribe
"cheaper gather" as an established bottleneck. Optimize only with numerical/gradient/reload parity.

## RP78 - Accounted workers, with no deployment disturbance

Both workers already have revision 1880caa in an isolated `predictor-rp73-reviewed` worktree. Existing
worker checkout paths remain unchanged. Fetch these orders and use a new version-pinned working
copy, or update an idle clean worktree after checking its usage; never switch code under an active job.
Prove one bounded synthetic governed child under each worker's own actor with CPU/RSS, terminal and
reconciliation. Then dispatch independent scientific cells by memory and remaining aggregate CPU.
Same environment/model implementation or a measured portability qualification is required for
paired interpretation. A copied commit alone is not a distributed execution measurement.

## RP79 - Bounded household DEV tier: architecture versus calendar

After RP74/RP77 and the accounted-worker acceptance pass, seal the following successor. State that
it is informed by prior DEV results, not confirmatory. Fixed: existing 10,020 evaluation identities,
target/horizon, common scaler, train 28d, W60, batch64, MAE+Adam lr0.003, validation every200 observed
updates, min_delta0, restore best, max4000 updates. **Patience10 events** gives 2,000 nonimproving
updates, approximately the former three training passes (~1,881), instead of only600. No per-arm
patience or budget tuning, and no promise that extending patience must improve error.

Primary factorial: {modular, adapted GRU} x {original inputs, real calendar}, three paired seeds
(12 cells). Secondary predeclared control: modular randomized-calendar, the same three seeds
(3 cells). Freeze all15 cells before scoring. This separates an architecture difference from an
input-information difference and retains the equal-capacity calendar check. Explicitly document
how adding channels changes parameters in both architectures; calibrate/init shared parts fairly.
Persist training/validation curves, optimizer iterations, exact feature support, best checkpoint,
budget trigger, and independent fresh-process weight reload predictions.

Run train-only cost pilots, projected at the full ceiling with corrected accounting and headroom.
Launch the complete15-cell block only if it fits the remaining envelope including closure reserve.
Otherwise preserve the sealed design/cost result and finish RP80-RP81; no outcome-driven pruning.
Do not automatically repeat Q3, the old pretraining factorial, or the full Q2 block. Cheap daily-lag
and exact-crop context contrasts remain next in queue, not secretly substituted into this question.

## RP80 - Financial handoff ready to execute, not a premature winner

Update FIN-LOSS-OPT with the repaired executable task and verified comparator path, weekly
train/validation/development-test identities, untouched final reserve, receiver architecture/input
context/receptive field, sufficient history/volume rationale, fixed-default 2x2 and matched tuning
budgets, causal train residual delta, optimizer decay rules, seeds, stop rule, and measurable cost.
Make explicit what the historical archive can and cannot support. Use primary literature/source
for model/optimizer/loss configuration provenance and mark adaptations. Electricity does not choose
the financial loss. Do not make universal claims that Huber wins at small residuals, or that AdamW
depends only on parameter count. Real-data scientific execution awaits this complete reviewed design;
all code, synthetic acceptance, metadata readiness and cost tooling are work to finish now.

## RP81 - One complete return and durable state

Update master plan, method state, coverage and orders together. Close all campaigns and reconcile
arrays/parent/accounting/warehouse by content, including failures. Preserve original evidence and
publish errata/qualified successor tables. Apply tests to the real paths with deliberately failing
controls, not only the provided probes. List exactly what was and was not independently verified.

Report MAE_z FIRST, its exact denominator, naive on the same origins, paired skill, matched
literature-derived reference and comparability class; published incompatible values only in notes.
Show seed-level differences, sample SD with ddof, time blocks, stop/censor status, observed cost and
all unsuccessful arms. Historical versus new measurements must be explicit. A calendar result is
not a finance result, and a lower forecast error is not a measured trading return.
State suite commands, environment/skip coverage, PRE/POST, commits, worker execution receipts,
remaining budget and pending independent work. Push completed work; one final audit request.
