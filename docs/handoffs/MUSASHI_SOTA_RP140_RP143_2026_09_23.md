# RP140-RP143: consolidate the reference while the next experiment is built

Review target: `satoshi/rp132-rp134-20260923@d0fb9acd`. These orders succeed
RP136-RP139; they do not restart its completed training or the existing executor.
Routine execution is authorized by the owner. No further "continue" permission.
Work concurrently in separate worktrees; external5090 remains first choice.

## Facts already resolved by Musashi

- A live authenticated warehouse read now matches the coordinator H192s2021
  preparation, terminal, design, prediction file, checkpoint and record, including
  byte counts. Evidence: `docs/audits/evidence/RP139_REVIEW_2026_09_23/LIVE_CUSTODY.json`.
  Its report's exact replay is preserved. This read is not a new inference,
  full-population closure or deletion certificate. No owner credential action.
- The existing service's configured environment supplies `DATA_GOV_LAKE_TOKEN`;
  use the narrow `check_live_custody.py --service-env` interface or existing
  environment support, never publish the token or copy a worker identity.
  Locate the environment via `systemctl --user show
  crispdm-data-warehouse-olap.service -p EnvironmentFiles`. Query is read-only.
- Continuation memory was raised LIVE from6GiB to7GiB, MemoryHigh6GiB, below
  the unchanged8GiB batch-slice cap. Available host RAM was about8GiB; other
  batch use about0.4GiB. No restart, training/code/data change, or OOM occurred.
  The reported5.80GB peak is5.40GiB, not5.80GiB. Retain all memory-pressure events.
- Six L512 records now exist with terminal receipts. H336s2021 is training;
  no additional executor or GPU job may overlap its ownership.

## RP140: finish protocol A's evidence composition, not a new training round

Consume the fresh custody audit and the four original-device replay reports.
Bind the complete objects by design, record, checkpoint, array, report and
accepted-terminal identity. Preserve original reports and publish an additive
successor through the existing governed evidence path. Test a foreign report,
changed checkpoint, missing terminal and missing population before accepting it.
Do not fabricate a current replay by editing a prior row's verified flag.

Distinguish exact numerical replay from measured training-device attribution:
the coordinator report currently says `trained_on_device_uuid=null` and
`trained_on_attribution=UNKNOWN`. Its predictions were reproduced exactly on
the4070; this alone cannot prove which physical UUID trained them. Bind original
execution evidence if available, otherwise retain UNKNOWN and qualify the
same-device phrase. Do not rerun training to manufacture historical provenance.

Consolidate the12-cell protocol-A population and generate normalized official
MSE/MAE, matched persistence/seasonal errors, seed dispersion, frozen agreement
rule and fidelity separately. A current closure must not discard independently
bound prior replay evidence merely because another GPU cannot reproduce each
element identically. Implement any composition change with negative tests;
never relax the original pointwise or score thresholds.

Retention remains held. Legacy estimator-definition limits do not block a score
whose own required arithmetic, targets, population and custody are verified.
Do not repeat fourteen-family reductions to improve unrelated retention prose.

## RP141: supervise and close B on its existing service

Keep `crispdm-rp135-continuation-20260923.service`, its real flock and immutable
producer checkout. The expired Hermes task remains blocked. Observe children,
authenticated receipts and the measured CPU ledger, not merely service state.
Continuation allocation remains24000CPU seconds/8h; prior costs are separate.
Before each remaining child assess remaining allocation using measured costs;
the heartbeat's wall timeout is not proof of aggregate CPU enforcement.

The7GiB runtime scope change is operational and does not alter sealed recipe.
Keep other jobs out of the shared8GiB slice unless combined admission fits.
Preserve failures and outbox conflicts, and reconcile accepted content without
refitting completed cells. After the finite fit queue, use the same5090 for its
replays and official metrics. No prediction deletion or compression this round.

Output all12 planned cells including failures/censoring. A-versus-B is a recipe
comparison, not an isolated context effect. Table9 lookback remains unresolved;
do not tune against public test scores or call it exact Table9 replication.

## RP142: make the next doctoral contrast executable in parallel

The preparation's internal-AE-validation paragraph repeats an obsolete rule.
For this successor, construct an internal chronological training/validation
partition entirely INSIDE outer TRAIN, purged by actual sample/target support.
Outer validation may select the downstream forecasting checkpoint as declared,
but is not AE training or AE early-stopping data. No outer-test access for any
selection. Prove this by row identities and future perturbations; labels being
absent is not evidence that a window belongs to TRAIN. Correct both the new
preparation and the inherited13E wording with a dated successor, not erased history.

Write the actual matched ECL intervention, not a transplant of household defaults:
enumerate321 channel roles/order, windows/horizons, tensor shapes at every module,
Conv1D detector depth/width/kernel/dilation/activation, fusion, temporal receiver,
readout, target transform and inverse, loss/optimizer/schedules and checkpoint rule.
The existing E1 pilot predicts one channel at one offset; it is NOT a matched
ECL adapter. Require the full `[batch, horizon, 321]` target and output, exact
channel ordering, author split/scaler and author reduction over the same windows,
steps and channels. Compare independently generated target identities and metrics
to the reference before fitting. No substituted one-step or univariate task.
Use the already approved modular architecture as the hypothesis, and the accepted
TimeFilter recipe as the reference; weak context/capacity cannot masquerade as an
extractor-pretraining effect. Measure effective temporal support by perturbation
and gradient; prove R0/R1/R2 initialization/freeze/update behavior and reload parity.
Keep grouping, fusion, receiver and head identical within the regime contrast.
Count unique observations, overlapping windows and updates separately. Include
AE cost and downstream cost, plus an equal-total-cost reading; no best-seed choice.
The former E1 defaults are not approved as an ECL configuration merely because
their plugin works. Include fixed validation masks, train-only normalization,
finite-target checks, measured receptive field and reload/metric parity.

Authorization NOW: implement and test this path, then execute one governed
TRAIN-ONLY resource/ML pilot once its input/shape/causality/regime tests pass.
Use WORKER_A after admission while B occupies external5090; no concurrent load on
its host. Pilot budget1800CPU seconds and1800wall seconds including AE plus R0
steps and diagnostics; prescribe the steps before execution from a cost probe,
no public-test scoring and no claim about H1. A pilot is not permission to shrink
the eventual reference or select a cheap weak model. Seal the scientific factorial
and paired seeds/budgets before any outer-validation model selection. The final
comparison requires acceptance of the reference it actually uses, not completion
of an unrelated retention certificate or an extra owner signature.

Protocol-B completion is not automatically a dependency of a contrast explicitly
matched to accepted protocol A. Choose this dependency in the design before scores,
not after selecting whichever reference appears easier to beat. Core-pretraining,
financial loss/optimizer and weekly RL remain separate mandatory plan stages.

## RP143: finish the handoff, no artificial sequential waiting

While the GPU trains, finish RP140 and RP142 in their own worktrees. Preserve the
repair branch and merge documentation deliberately; never pull code under an
active training child. Prepare the financial producer-contract investigation and
matched reference design concurrently without reading the closed reserve.

Every return leads with new measured results (official normalized metric, same-row
naive and literature scope), completed/remaining cells, scientific interpretation,
CPU/GPU cost and live ownership. Report observed scores versus accepted scores
separately. End only after all independent assigned work is done or an actual
external fact prevents it; no routine approval requests. Do not invent scientific
progress if the active fit has not finished. No owner action is currently needed.
