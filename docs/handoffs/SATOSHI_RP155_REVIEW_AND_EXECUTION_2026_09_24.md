# RP155 continuation: correct scoring while shipping the real application slice

**RP156 update:** read the [b3a064d5 review and immediate continuation](../audits/work_plan/MUSASHI_RP156_REVIEW_2026_09_24.md)
first. Target-support and named reductions are repaired; keep the corrected scores.
Finish authority/child-result validation using retained evidence, not another
routine inference loop. The real Laya pilot and application lanes remain due now.

Start here, then the [all-lane assignment index](SATOSHI_ACTIVE_ASSIGNMENTS_2026_09_24.md).
This continues existing orders and budgets, not a new permission round. Reviewed
return: `ebe35fb3`, experimental delivery `99a1630a`. Musashi implements probes
and review; Satoshi owns runtime fixes, inference and application implementation.

## 0. Integrate the orders that the latest return does not contain

At review, neither `032aaa52` nor `7e924bf5` is an ancestor of `ebe35fb3`.
Read/incorporate both, plus this successor, preserving newer work. The five older
M5PHET findings being repaired does NOT close the three reproduced d9ffc4d gaps.
Do not ask the owner to reconstruct which instructions were delivered. Refresh
current leases, child identities, worktrees and remaining budgets before dispatch;
the 06:46Z idle report is a snapshot, not continuing authority.

## 1. Experimental lane: no retraining, repair and re-score

Freeze the [RP155 review probes](../audits/evidence/RP155_MUSASHI/probe.py) unchanged
as observed PRE, and write independent expected-outcome POST tests first.

- Derive forecast target support from actual monitor origins and horizon. Keep
  full validation and the old 1,897-origin complement as historical descriptive
  populations. Seal a scoring-only amendment for the **1,802 label-disjoint
  windows** (local origins 735..2536), all three regimes/seeds, before reading their
  new scores. Verify support by actual row/channel identities. Do not call temporal
  dependence solved by this correction or tune the boundary for a favorable score.
- Bind scoring to registered design, exact seed/regime population, data/scaler,
  channel order, horizon, selected checkpoint and declared reduction. Fail before
  accepted aggregation on missing/foreign/duplicate cells, changed checkpoint,
  wrong data/horizon, nonfinite arrays or missing returned population. Keep partial
  diagnostics explicit, not silently averaged into a completed paired contrast.
- Use the existing bounded author float32 reducer and retain named float64
  diagnostics. Recompute same-origin persistence, seed-paired differences and
  complete population counts. Existing selected checkpoints are sufficient.
- Measure fresh-process prediction/reduction checks at their actual scope; do not
  equate checkpoint digest equality alone with output parity. Make validation-only
  access explicit in the loader. Document the actual test-access trace, without
  asserting accidental loading means test-driven optimization.
- Supply fit + all scoring/replay/closure costs from actual process ledgers. Admit
  the inference-only successor under the EXISTING remaining allocation; no reset.
  If insufficient, report the measured shortfall and continue the application lane.
- Evaluate the existing accepted TimeFilter H96 checkpoint(s) on the same origins
  only within remaining admitted resources; disclose its selection exposure and
  classify this as a re-evaluated reference, not its published test score. If not
  available/admissible, name the missing comparator rather than substitute a paper
  test number. Do not train another reference in this continuation.
- Publish an additive corrected scoring report and verify persisted content via
  the existing accounting route. Preserve original artifacts and make the erratum
  adjacent. H1 remains undecided; no financial claim follows from ECL improvement.

## 2. Real Laya is the next executable GPU application task, not another proposal

RP147 already authorized one pinned-checkpoint pilot (600 CPU s / 900 wall s
after setup). Run its actual disk/network/device admission and setup now in an
isolated environment; queue it on an eligible resource without waiting for the
CPU scoring repairs. External 5090 remains first choice for general GPU work.
The specifically authorized coordinator residency measurement is a distinct host
fit test, not permission to silently change a sealed device or run all devices.

Reuse the reviewed upstream SDK/encoder; record exact selected SDK/weight revisions
and compatibility decisions. Do not recreate the model or add an LLM orchestrator.
Deliver **real text -> SDK output -> M5PHET typed result -> news-signal local
evidence**, with direct-SDK parity, explicit task/payload validation, uncertainty
scope, memory and p50/p95 latency. A mock-only suite does not complete RP147.
No calibrated trading probability or broker authority is inferred from the output.

## 3. Parallel CPU/application lanes continue immediately

Delegate independent work to available Hermes agents in separate worktrees where
useful, with one integration owner and existing CPU limits. Do not give secrets
in prompts or create duplicate experimental executors.

- M5PHET: the three d9ffc4d repairs and real optional governance/DOIN integration
  stay assigned. They do not hold the SDK installation or application code lanes.
- News/calendar: implement receipt/revision/dedup persistence and stale-input
  handling; inventory existing economic data and build vintage-aware as-of
  transforms with CAL01-CAL12. Real feed capture requires verified entitlement,
  but absence of a feed does not prevent those bounded code tests.
- MT5 demo and Alpaca paper: implement interfaces and refusal/recovery paths now.
  Verify existing demo/paper account/risk mandates before canaries; use existing
  risk/bridge routes. No real-capital action, active-strategy replacement or direct
  transport bypass is authorized. Report each broker separately.
- Finance: do the assigned producer/source investigation and matched-reference
  preparation. Missing historical evidence is not an authorization the owner can
  magically supply: name which fields lack support and whether an evidence-backed
  archive-only contract or a source successor serves the declared task. Never
  invent receipt/completion clocks to pass HTTP 422. Keep the private reserve
  untouched and FIN-LOSS-OPT fitting gated, not the entire finance code lane.

## 4. Completion is per delivery, not per status-table publication

RP151's table is received; the overall assignment is NOT finished while executable
RP147-RP150 work remains unstarted. Progress reports are fine, but do not finish
the turn solely to ask for permission already present. Do not label unperformed
work complete to satisfy this instruction either.

Next return starts with corrected experimental numbers OR the precise running
job, then the real Laya pilot outcome and one evidence-linked row per remaining
lane. Every metric table includes scale, population, naive and reference with
comparability qualified. Identify genuine missing entitlement/resource by object;
keep independent lanes progressing. No new scientific fit, compute allocation,
blanket re-audit, model redesign or owner decision is introduced here.
