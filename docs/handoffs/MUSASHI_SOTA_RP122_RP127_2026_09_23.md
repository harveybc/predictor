# Satoshi: RP122-RP127, finish the finite acceptance work

Base reviewed: `316c67af`. Read the [independent review](../audits/work_plan/MUSASHI_RP114_RP121_REVIEW_2026_09_23.md)
and its executable probe/results. Supersedes RP114-RP121. Complete all feasible
blocks and publish one return. No per-step owner permission requests. Existing
authorization covers these repairs and bounded governed verification, not
bypassing thermal, scientific or retention prerequisites.

No benchmark training, new seeds, tuned tolerances, B factorial, financial fits,
holdout exploration or service maintenance is required. Preserve all historical
measurements. Only the external RTX 5090 is GPU-eligible after the established
physical-UUID, thermal, connection, RAM/VRAM/disk and competing-work checks.
It stays first choice. Bound host CPU work and monitor it independently; no
fallback to internal GPUs, the held worker or the traveling coordinator.
Use the standing 14,400 CPU-second round ceiling, count worker time, and preflight
actual storage with its operating reserve. No assumed additional disk or NAS.

## RP122: freeze the new PRE before repair

Preserve the supplied probe and results without altering them. Add tests on real
entry points for (1) plausible zero ACF/quantiles plus a missing defined
correlation, (2) a locally relabeled diagnostic artifact with immutable accepted
terminal, and (3) deletion through `require_acceptance=False` with no accepted
chain. Keep positive controls. Mutation tests must exercise individual metric
families, not merely the combined example. Write acceptance criteria before code.

## RP123: require the accepted relationship, not just accepted bytes

Bind evidence kind, subject/cell, design/run and artifact role to the authoritative
accepted terminal and its receipt. The registry is a lookup hint, never the
source of those facts. A digest stored in an unrelated role or for another
subject must not certify a closure or regeneration. Validate the relationship
between a regeneration record and its acceptance, not just two unrelated hashes.

Cover missing/contradictory kind, subject, role, design, duplicate conflicting
artifacts and mismatched receipts in the actual historical/deletion consumers.
Use the existing terminal contract rather than inventing a parallel authority.
Any legacy terminal lacking the needed binding stays explicitly qualified until
a dated successor is validated and published; never rewrite it retroactively.

## RP124: implement numerical references for the declared catalog

Inventory the finite approved catalog, not "all possible metrics". For each
estimator state its input population, axis, orientation, reduction, parameters,
undefined behavior and numerical/approximation tolerance before measurement.
Use an independent implementation on the SAME population and definition:
ACF over origin time per fixed forecast step must not be compared to concatenated
steps of sampled windows. Match centering, denominator, lags and channel reduction.
Check histogram-derived quantiles against that declared approximation, not exact
quantiles silently substituted for a different estimator. Independently check
baseline values, not only identities involving the producer's own baseline.

A value present on one side and missing on the other is a disagreement unless
the declared mathematical undefined condition is independently demonstrated.
Null, empty lists, booleans and NaN must not silently become zero discrepancy.
Include constant/short sequences, boundary bins, periodic and deterministic
residuals, and plausible in-range wrong values per family. A domain test remains
additional evidence, not a numeric reference. Emit coverage by field/family and
refuse a claim of full independent acceptance if any required field is unchecked.

## RP125: remove the destructive acceptance bypass

Make accepted-chain and durable-backup verification mandatory in every callable
that can unlink predictions, including resumption. Remove the public bypass;
tests inject faithful fixture authority instead of disabling prerequisites.
Keep dry-run non-destructive and accurately report failed prerequisites.
Reproduce default refusal and prove no alternative public invocation removes the
fixture without the required accepted relationship. Preserve content identity,
copy inventory, aliases, partial recovery and backup-destination-byte tests.
Do not weaken the existing reader/writer exclusion or thermal retention policy.

## RP126: finish catalog evidence for the twelve retained model cells

After the fixes and tests pass, apply corrected checks to the three retained H96
arrays. For the nine deleted cells, reuse independently sufficient retained
material where possible; otherwise regenerate by inference from the original
checkpoint on the admitted 5090, one bounded cell at a time, and compare original
prediction/target identities before numerical certification. No refitting.
Recheck the six formerly called independent as well as the other six: the old
checker did not cover all promised families. Report exact per-cell/per-family
coverage and any genuine unresolvable mismatch without manufacturing acceptance.

Keep official author-float32 scores and separate float64 checks. Verify content
against the live accepted chain after publishing dated successor evidence.
Compare old/new estimator values and score impacts explicitly. No claim of
changed real metrics unless measured. Preserve old evidence with its old scope.
Clean regenerated scratch only after corrected acceptance, verified metadata
backup and content-specific receipts; do not accumulate nine large arrays.
The three original H96 arrays remain retained while their replay gate is open.

## RP127: one scoped closure and return

Re-run the supplied PRE/POST and focused suites; record interpreter, dependency
and skipped-test scope, cost and admitted device evidence. Update method state
incrementally and correct the RP121 blanket "every family"/"cannot be skipped"
claims by dated errata, not deletion of history. Generate the owner table from
the verified retained source: official normalized MSE/MAE, matched naive and
seasonal baseline, published comparison, seeds, scope and agreement criteria.
Separate score/replay/custody/catalog status; no verified four-horizon mean with
an unverified member. Preserve the previous operational scores without retraining.

WORKER_A original-device replay remains conditional on explicit restored-cooling
confirmation and fresh admission. If received while working, execute the bounded
replay against the frozen criterion and close from its result. Otherwise finish
all independent work and name only that physical hold, not another request for
engineering authorization. The estimated 66 seconds is not guaranteed acceptance.
Do not widen the cross-device rule or rewrite historical training attribution.
Keep protocol B at measured resource-planning scope; do not repeat its pilot.

Publish one return, review request, commit/push and worker synchronization.
Stop for review after the finite work, not after each successful step.
