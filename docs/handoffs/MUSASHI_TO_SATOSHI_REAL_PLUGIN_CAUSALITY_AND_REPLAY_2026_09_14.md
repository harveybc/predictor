# Satoshi: real-plugin causal validation and reproducible offline adoption

Date: 2026-09-14. Issued by Musashi under the owner's standing authorization.
Start now. No additional owner approval is required for the bounded work below.

Context: [independent review](../audits/work_plan/MUSASHI_REVIEW_STANDING_ORDERS_2026_09_14.md),
[previous order](MUSASHI_TO_SATOSHI_CAUSAL_PIPELINE_AND_OFFLINE_DOIN_2026_09_14.md),
and [work plan](../integracion_workplan_2026_09_10/09_ADOPCION_DATA_LAKE_DATA_WAREHOUSE_2026_09_14.md).

Do not repeat the catalogue activation or the accepted four-consumer production
proof. Do not restart production services, rewrite history, reopen B4, change D2
decisions, start D3, use GPUs, publish DOIN genes or contact a trading venue.
These are CPU mechanical/data-contract tasks, not new scientific selection.

## Execution order

R1 then R2/R3, then R4; R5/R6 may run independently in parallel where memory permits.
Use isolated worktrees and existing per-application environments. Snapshot your own
starting diffs; never discard another agent's work. Investigate the previously
reported work-plan loss using editor history/backups without overwriting anything;
report an unrecoverable document honestly rather than reconstructing it as history.

Use the existing test-led method: requirements -> failing production-path tests ->
small implementation -> integration -> alpha acceptance. Maintain one compact
requirement/test/function/evidence matrix and the existing method state. Do not
create another competing project plan.

## R1. Correct column roles at the actual loaders

- Reproduce both review examples before fixing. Require a literal boolean opt-in
  for target/feature overlap; absence is not permission. Reject contradictory
  metadata/time/feature roles, duplicate declarations and malformed role lists.
- Test through each consumer's real loader, not only a copied helper. Inventory
  predictor, preprocessor, feature-eng and feature-extractor separately; prove
  which implementation each uses and apply the same semantics without inventing
  an unnecessary shared package.
- Distinguish historical target-as-input from a future label. Tests use row IDs,
  timestamps, decision time and target horizon, including repeated equal values.
- Explicitly derived calendar features are allowed under their own feature names;
  raw availability metadata must not become model input by conflicting declaration.
- Preserve explicit legacy migration for reproduction, but do not treat it as
  scientific eligibility. Bind the resolved role plan to the outcome artifact.

## R2. Exercise the real transformations and feature engineering

- Retain toy controls as controls and correct their overstated documentation.
  Enumerate the plugins actually selected by the four smoke configurations and
  the next proposed causal configuration. Import and execute those entry points.
- On one deterministic chronological trajectory, run actual feature-eng indicators,
  real scaler fitting, preprocessing/window construction and extractor alignment.
  Check output identity/timestamps, not merely shape or unequal numeric values.
- Prove train-only fitting and prefix invariance by changing future observations,
  future targets and future missingness without changing the admissible prefix.
  Test append-only extension, internal missing intervals and train/cal/test borders.
- For each actual wavelet/STL/filter path, test boundary extension, padding and
  reconstruction on full series versus admissible prefixes. A three-tap substitute
  does not test that implementation. A noncausal operator can remain an offline
  oracle, but cannot be admitted to a causal configuration under a renamed label.
- Record lookahead, warm-up, output availability and response characteristics
  separately. Do not promise noise removal with zero delay. Test impulses, steps,
  changing frequencies and discontinuities; first response is not group delay.
- Deliberately mutate the productive scaler/window/filter path and show that the
  same integration tests fail. Scope state/restart tests to APIs that exist.
- Repair fixture regeneration: manifest-only checkout, missing artifact, mismatched
  artifact and fresh checkout must each yield regeneration into temporary storage
  or an explicit diagnostic, never accidental FileNotFoundError. Verify generated
  identities against the published manifest; never overwrite original evidence.

## R3. Make the agent-multi replay reproducible and metrics factual

- Commit the existing runner fix in an isolated branch with its tests. Do not
  clean the active agent-multi tree by resetting others' changes. Link retained
  historical dirty-run receipts as limited evidence; never rewrite their identity.
- Bind template, runtime overlay, resolved config, executable source and input
  digest to the run. Use per-run outputs; remove the repository-global config
  fallback as a source of measured metrics.
- Separate requested timesteps from observed steps/updates. Obtain actual counts
  from the real runtime. Missing observation is missing, not the requested value.
  Preserve legitimate partial/failure outcomes with measured cost.
- Seed a stale summary, stop a run early, omit its summary and alter only its
  overlay in regression tests. None may report stale or configured counts as
  completed work. Required mechanical evidence must be explicit before dispatch.
- Demonstrate the new wrapper's real stranded outbox recovery on a disposable
  stack: target unavailable -> pending item -> restored target -> exactly one
  row -> second flush sends nothing. For shared wrappers, prove actual delegation
  and exercise the relevant entry points rather than duplicating an entire suite.
- After these pass, execute ONE corrected NON_GOVERNING CPU replay against the
  existing production fixture/services: <=64 requested steps, <=10 minutes,
  <=2 GiB process memory. Record actual work and reconcile receipt/cube. Do not
  induce production outages. This order authorizes that bounded mechanical run.
- Call the result governed agent-multi offline adoption. Separately document what
  distributed DOIN execution still lacks; do not claim it from this offline test.

## R4. Close archive semantics end to end

Use a disposable stack and the proposed ARCHIVE_RETROSPECTIVE contract. Exercise
provider -> data-lake -> data-gov -> receipt -> warehouse with UNKNOWN retained.
Full archive delivery may be supported; ranges, holdout-incompatible delivery,
point-in-time and live uses must reject for the actual declared reason. This does
not install or license the real financial resource. Additive schema changes must
retain compatibility for the already deployed synthetic contracts.

## R5. Finish primary-source research without an invented owner blocker

- Identify the actual producer code/log and bind its file-grain reception evidence
  to the resource version. Do not upgrade it to per-row publication/finality.
- Read and cite the official API documentation and applicable market-data terms.
  Separate internal analysis, derived results, raw redistribution and commercial
  use. If the applicable terms cannot be established, state precisely what is
  missing; do not infer rights from public HTTP access or the owner's approval.
- A public read-only comparison is authorized: at most 3 requests, 1000 bars each,
  5 MiB total, timeout <=30 seconds/request, no retries after a provider denial.
  Declare windows before requesting, covering the anomalous and ordinary bars.
  No account keys, trades or changes to the original lake. If endpoint terms do not
  permit the request, document that and continue all independent tasks.
- Register the acquisition through the existing governance accounting mechanism,
  preserve responses separately, record request/receipt times and hashes, and
  compare by bar identity. No difference proves neither finality nor no revisions;
  a difference needs field-level reporting and possible construction causes.

## R6. Reporting and acceptance

Persist experiment outcomes, failures and inconclusives through governance/OLAP;
unit tests may remain local. Inspect current services and cube read-only rather
than recycling old counts. Keep metric observations distinct from configuration.

The return must give exact reviewed/pushed revisions per repository, real tests
and omissions, causal plugin coverage, new terminal identities and reconciliation,
archive outcomes, and any actual owner-only action. All technical corrections
above belong to Satoshi, not the owner. Publish this round's evidence and update
the work plan incrementally; no historical outcome receives a scientific promotion.
