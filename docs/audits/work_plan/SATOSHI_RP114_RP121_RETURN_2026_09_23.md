# Satoshi — RP114–RP121 return: acceptance boundaries closed, the author's metric exact, the twelve cells restated

Orders: [MUSASHI_SOTA_RP114_RP121_2026_09_23](../../handoffs/MUSASHI_SOTA_RP114_RP121_2026_09_23.md) (a8dcbfce), on the
[RP106–RP113 review](MUSASHI_RP106_RP113_REVIEW_2026_09_23.md) of e145303c. Commits after a8dcbfce: ddfc867f … 0d5eefe9.

**Placement.** The only device used for new GPU work was WORKER_B's EXTERNAL RTX 5090 (UUID a9f35631…), admitted before every
dispatch and asserted inside each child process; WORKER_A's GPU and WORKER_B's internal RTX 5070 Ti stayed held, and nothing fell
back to them or to the coordinator. Host CPU work ran single-threaded inside governed slices with the host's own temperature
watched beside the GPU's: the host stayed at 42–65 °C and the 5090 at 35–41 °C. Most of this round's wall time is bounded CPU
work — catalog recomputation and independent estimator checks — with the GPU used in short bursts for inference and replays.

## Owner-facing table — official normalized MSE / MAE first

Population: 12 cells (L = 96 × T ∈ {96, 192, 336, 720} × seeds {2021, 2022, 2023}), design 9b49010d…, official processed ECL
(TSL, electricity.csv sha256 7e45845d…), TimeFilter @ dffde87e, the author's recipe and epochs, metrics in the normalized space
through **the author's own float32 reduction, reproduced bit for bit** (route `df_sota_author_metric_exact.v2`). Frozen
operational margin: |mean − published| ≤ 2·σ_paper + 0.0005 = 0.0105 MSE / 0.0125 MAE — a predeclared operational band, **not**
statistical equivalence and not exact equality to a rounded paper table. Closure report sha256 9c491f38…
([REPORT](../evidence/d3_k5_20260917/RP119/REPORT.gamma.json), [table](../evidence/d3_k5_20260917/RP119/SOTA_TABLE.gamma.md)),
generated together with the numbers quoted in this narrative.

| T | published MSE / MAE | official author-float32 per seed 2021 / 2022 / 2023 | mean (SD across seeds, n) | difference | matched persistence / seasonal-24 (z-MSE / z-MAE) | measurement | custody | original scorer | catalog acceptance | replay | device attribution | agreement |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 96 | 0.133 / 0.230 | 0.13332 / 0.14008 / 0.13309 ; 0.23055 / 0.23778 / 0.23032 | **not pooled** (measured mean 0.13550 / 0.23288 shown apart) | — | 1.5878 / 0.9455 ; 0.3211 / 0.3258 (5165 windows) | **MEASURED_REPLAY_UNVERIFIED** | ACCEPTED_ARTIFACT_CHAIN | author float32, exact route | all fifteen domain checks pass on each of the three cells and **every estimator family agrees with an independent recomputation over 159,164,640 elements to 0.0**; the acceptance still refuses on the missing replay | **FAIL** cross-device on the 5090: max\|Δ\| 1.9e-4 / 2.2e-4 / 2.7e-4 > atol/rtol 1e-4, replayed author metrics bit-identical; preserved same-device evidence on WORKER_A is HISTORICAL_RECORD_ONLY | INFERRED (WORKER_A, 4090 Laptop) | UNVERIFIED: the frozen rule is not widened |
| 192 | 0.154 / 0.248 | 0.16086 / 0.15863 / 0.15345 ; 0.25599 / 0.25243 / 0.24807 | **0.157645 (0.00380) / 0.252163 (0.00396), n = 3** | +0.00365 / +0.00416 | 1.5962 / 0.9507 ; 0.3043 / 0.3237 (5069 windows) | VERIFIED | ACCEPTED_ARTIFACT_CHAIN; predictions deleted 22-sep under authorization | author float32 of the original closure, bound by digest to an **accepted** report (fbe2cd4c…) | accepted; seed 2022 also **independently recomputed from regenerated bit-identical bytes** (worst family disagreement 2.2e-16) | historical: same-device PASS before deletion for s2022/s2023, cross-device PASS for s2021 | s2021 INFERRED (coordinator, 4070 Laptop), s2022/s2023 INFERRED (WORKER_B 5090) | **OPERATIONAL_AGREEMENT** |
| 336 | 0.162 / 0.261 | 0.16259 / 0.16817 / 0.16267 ; 0.26067 / 0.26741 / 0.26087 | **0.164480 (0.00320) / 0.262985 (0.00384), n = 3** | +0.00248 / +0.00198 | 1.6178 / 0.9613 ; 0.3273 / 0.3427 (4925 windows) | VERIFIED | ACCEPTED_ARTIFACT_CHAIN; predictions deleted 22-sep | author float32 **recovered after deletion**: inference from the retained checkpoints reproduced the predictions bit-identically and the corrected route scored them | accepted; seed 2021 also independently recomputed from regenerated bytes (1.1e-16) | historical: same-device PASS before deletion | INFERRED (WORKER_B 5090) | **OPERATIONAL_AGREEMENT** |
| 720 | 0.184 / 0.284 | 0.18714 / 0.19817 / 0.18537 ; 0.28752 / 0.29865 / 0.28568 | **0.190226 (0.00694) / 0.290617 (0.00702), n = 3** | +0.00623 / +0.00662 | 1.6468 / 0.9754 ; 0.3670 / 0.3733 (4541 windows) | VERIFIED | ACCEPTED_ARTIFACT_CHAIN; predictions deleted 23-sep | author float32 recovered the same way | accepted; seed 2021 also independently recomputed from regenerated bytes (4.4e-16) | replays bit-identical (max\|Δ\| 0.0) before deletion | INFERRED (WORKER_B 5090) | **OPERATIONAL_AGREEMENT** |
| avg | 0.158 / 0.256 | — | **NOT_COMPUTED** over a denominator of four; T = 96 is missing for every seed and is named, not dropped | — | — | — | — | — | — | — | — | — |

Seed dispersion is reported as the sample SD across the three seeds of each horizon and its host scope is stated: T = 192 mixes
two hosts (the coordinator's 4070 Laptop for seed 2021 and WORKER_B's 5090 for the other two), so its 0.0038 / 0.0040 is not pure
seed variability; T = 336 and T = 720 are single-host. Paired seed contrasts from the persisted per-window series are DONE for
T = 192, 336 and 720 and NOT_APPLICABLE for T = 96
([PAIRED_CONTRASTS](../evidence/d3_k5_20260917/RP119/PAIRED_CONTRASTS.gamma.json)). Independently checked float64 reductions
accompany every cell in its catalog; in the regenerations accepted this round they agree with the author's float32 reduction
within 3.6e-8, and they are reported as a separate named check, never under the author's field.

## What was done, per block

* **RP114 — the five counterexamples frozen before the repair.** Musashi's probe is preserved unchanged
  ([musashi_probe_unchanged.py](../evidence/d3_k5_20260917/RP114/musashi_probe_unchanged.py)) and run on WORKER_A at the reviewed
  base as the PRE ([PROBE_RESULTS_PRE.json](../evidence/d3_k5_20260917/RP114/PROBE_RESULTS_PRE.json)): the rewritten report was
  historically verified with a score of zero, the fabricated regeneration likewise, the deletion without approval or backup
  completed and removed the only array, the deliberately broken catalog producer was accepted with every oracle at zero, and the
  float32 route disagreed with the author's function at N = 16,777,217. The same probe, unchanged, at the repaired head is the
  POST ([PROBE_RESULTS_POST.json](../evidence/d3_k5_20260917/RP114/PROBE_RESULTS_POST.json)): HISTORY_REPORT_NOT_ACCEPTED with no
  score, the fabrication refused while the ORIGINAL accepted score stands, the deletion REFUSED with the array retained, the
  catalog refused on four domain violations, and float32 parity restored. Every scenario is also a frozen test on the real entry
  points, extended beyond the probe's exact values: non-finite and missing evidence, a CPU versus CUDA regeneration, a backup
  whose destination is empty or corrupt, an acceptance that does not match the catalog on disk, and denominators swept across the
  2^24 boundary and the real benchmark cardinalities.
* **RP115 — acceptance, not resolution.** Closure reports, catalog acceptances and regeneration records are now published as
  governed COMPLETED terminals carrying their digests, and a local registry only says WHICH unit to ask about: the answer comes
  from the receipt and the warehouse row. Historical verification requires the report a deletion marker names to BE accepted
  evidence, so rewriting it and recomputing the pointer refuses. A regeneration must have both its record and its acceptance
  accepted AND be consistent with what is retained — identity, the record's digests, the device it ran on (a CUDA regeneration
  must name its measured UUID), a finite metric, the catalog's population, and agreement with the retained catalog's global
  metrics; an acceptance that merely says `pass` is refused. A refused successor is reported and ignored: it never replaces a
  score and never destroys the original binding. The two retained closure reports the markers name were given clearly dated
  successor acceptances (fbe2cd4c… of 22-sep and bc9d3388… of 23-sep), each carrying the explicit scope that acceptance begins at
  that date and that earlier local availability proves nothing about earlier acceptance.
* **RP116 — prerequisites that cannot be skipped.** The deletion now refuses without the accepted closure report digest, without
  an accepted catalog acceptance of the catalog actually on disk, or without a backup whose DESTINATION BYTES exist and hash to
  the manifest; the manifest must cover the unit's record, catalog and acceptance. This holds through the API and the CLI: the
  bare call Musashi used now refuses and keeps the array. The unlink boundary no longer trusts a path: the bytes are hashed
  through an open descriptor, the path is confirmed to still name that inode, the unlink happens, and the same descriptor is
  hashed again afterwards, so each receipt line states whether the bytes removed were the accepted bytes. A cooperative lock
  cannot bind a foreign writer and is not claimed to; what is claimed is what the descriptor proves.
* **RP117 — every family checked against its own definition.** The catalog acceptance now validates each claimed family's domain
  (a standard deviation that is non-negative and equals sqrt(var), an entropy within log2 of its bin count, a 64×64 mutual
  information within 6 bits, autocorrelations and correlations within [-1, 1], R² at most 1, monotone quantiles, histogram counts
  that sum to the population, and every reported number finite) and, wherever the prediction arrays still exist, recomputes
  residual moments, entropy, joint mutual information, correlation, R², errors and the histogram with a SECOND implementation
  that never calls the producer. Musashi's injected producer — SD = -123, entropy = 999 bits, MI = 999 bits, ACF = 42 — is refused
  on four domain violations and on three independent disagreements. Where the arrays were deleted the acceptance says so
  explicitly as a historical limit instead of implying certification.
* **RP118 — the author's mean, exactly.** NumPy's `mean` on a float32 array divides the float32 sum by the element count as an
  int64 scalar, which promotes the division to float64 and casts the quotient back; dividing by float32(N) is a different
  operation whenever N is not exactly representable. The route is corrected and versioned v2, v1's claim retained as superseded,
  and the denominator, the pairwise sum and the whole function are tested separately — including the real cardinalities, of which
  **T = 336 (531,190,800) and T = 720 (1,049,515,920) are NOT exactly representable in float32** while T = 96 and T = 192 are.
  Those six cells were therefore rescored, by regenerating their predictions from the retained checkpoints on the admitted 5090:
  all six came back BIT-IDENTICAL to the deleted originals and their catalogs re-derived equal to the retained ones.
  **Per-cell impact, measured before any claim:** of the twelve cells, exactly one number
  changed — T = 336 seed 2021's MAE, from 0.2606664299964905 to 0.2606664001941681, a difference of 3.0e-8. Every other
  author-float32 value of T = 336 and T = 720 is bit-identical under both routes, and T = 96 and T = 192 could not be affected.
  No horizon mean, no agreement verdict and no scientific conclusion moves; what changes is that the number is now the author's
  arithmetic rather than anear-equal approximation of it.
* **RP119 — the successor closure, with every property stated apart.** All twelve cells were closed again on WORKER_B with the
  replays restricted to the admitted 5090. Each row now carries, separately: whether it was MEASURED, whether its custody is the
  accepted artifact chain, which scorer produced its number, whether its catalog was independently accepted, what its replay
  established, and the evidence class of its device attribution. T = 96 is presented as **MEASURED_REPLAY_UNVERIFIED** rather
  than "no measurement": its values are shown with an explicit `pooled: false` and the reason is the missing accepted replay, not
  a missing measurement. Nothing unanchored enters an aggregate, the four-horizon average keeps its denominator of four and names
  the horizon missing per seed, and the operational margin is never described as statistical equivalence. Old reports and
  receipts are kept; this closure is a successor, not a rewrite. The result: **T = 192 (0.157645 / 0.252163), T = 336 (0.164480 / 0.262985)
  and T = 720 (0.190226 / 0.290617) are in OPERATIONAL_AGREEMENT** with the published values on the author's own metric, and
  T = 96 (0.13332 / 0.14008 / 0.13309) is measured and unverified. All twelve catalogs were independently accepted and published
  as governed evidence; the worst internal oracle across the whole population is 1.1e-16, and the accepted-evidence registry holds
  39 entries (3 closure reports, 18 catalog acceptances, 18 regeneration records). Run ledger: 19 attempts (12 with
  records, 7 retired or interrupted and carried with an explicit NONE_RECORDED cost), 5.37 h of measured wall time, 210.3 GiB
  free disk measured after the cleanup. No production array was deleted in this round: the three T = 96 arrays stay because they
  are unverified, and the regenerated temporaries are removed by their own acceptance.
* **RP120 — protocol B planned honestly.** Two claims in my earlier cost document were wrong and are corrected against the
  pinned code rather than comments: the patch length grows with the input, so protocol B has four patches per channel against
  protocol A's three (tokens 963 → 1284, mask 2.78 M → 4.95 M elements), not 5.3 times the tokens; and the custom loader
  prepends `seq_len` rows at the test border, so the test population is `num_test − pred_len + 1` in both protocols — identical
  populations, with only the training windows shrinking by about 2 %. The withdrawn "2–4×, 6–16 h" estimate was replaced by a
  **bounded, governed, exact-recipe pilot** on the admitted 5090: 0.0555 s (T = 96) and 0.0602 s (T = 720) per optimizer step,
  6.6 GiB peak VRAM, 2.2 GiB peak host RSS, 1074–1113 steps per epoch, measured with no test score, no checkpoint and no
  selection. Projected: about 0.33 h per cell at fifteen epochs and **3.9–4.0 h for the twelve-cell population**, comparable to
  protocol A rather than several times it — with early stopping, the evaluation and catalog work, warm-up variance and the two
  unmeasured horizons named as excluded. No protocol B campaign was launched.


## Exact remaining deficits

1. **T = 96 is measured, not verified.** Its three cells have accepted custody, the author's own float32 scores, catalogs whose
   every estimator family agrees with an independent recomputation over 159,164,640 elements, and preserved same-device replay
   evidence from the device that trained them. What they lack is an accepted replay under the frozen rule in a current closure:
   on the admitted 5090 the pointwise rule fails cross-device (max |Δ| 1.9e-4 / 2.2e-4 / 2.7e-4 against atol/rtol 1e-4) while the
   replayed author metrics are bit-identical. The rule is not widened. Verification needs about 66 s on WORKER_A's GPU, held
   until cooling is confirmed. The row is reported MEASURED_REPLAY_UNVERIFIED and never pooled.
2. **The four-horizon average stays NOT_COMPUTED** while T = 96 is unverified; the denominator remains four and the missing
   horizon is named per seed rather than dropped.
3. **Legacy device attribution cannot be repaired.** The nine GPU-trained cells of T = 192/336/720 carry INFERRED or
   VISIBILITY_MASK attribution because their records predate the measured-UUID field; a new measured replay or regeneration on
   the 5090 does not retroactively measure the device that trained them, and no cell was retrained to manufacture one. Only the
   work done from 2026-09-22 on records a MEASURED UUID.
4. **Independent estimator validation covers six of twelve cells on real bytes.** The three T = 96 cells still hold their
   arrays; one cell per deleted horizon (T = 192 seed 2022, T = 336 seed 2021, T = 720 seed 2021) was regenerated from its
   retained checkpoint on the admitted 5090, came back BIT-IDENTICAL, and had its retained catalog checked family by family
   against the independent implementation — worst disagreement 4.4e-16, i.e. machine epsilon — after which the temporaries were
   removed. The remaining six deleted cells carry domain validation and internal oracles with an explicit historical limit;
   lifting it for each is a further ~6 minutes of admitted work per cell, not a new claim.
5. **Protocol B is measured for cost only.** No protocol B cell has been trained or scored, and the per-horizon input length
   behind each published Table 9 column remains unpublished, so no unqualified Table 9 claim is possible from an L = 512 run.
6. **The coordinator's and WORKER_A's copies of the T = 96 arrays** are inventoried and untouched under the thermal policy.


## Suites, with scope and accounting

| suite | result | wall | peak RSS |
|---|---|---|---|
| `tests/test_df_sota_repro.py` (own process, WORKER_A) | 84 passed, 1 skipped | 65.3 s | 1.37 GB |
| closure table + benchmark contract + block + financial acceptance | 117 passed, 2 deselected | 161.5 s | 2.96 GB |
| `olap/store/tests` on SQLite **and** DuckDB | 106 passed, 1 skipped | 3.6 s | 0.22 GB |
| lake adopters | 4 skipped | 0.2 s | — |

Interpreter 3.12.13 with numpy 2.5.1, pandas 3.0.3, torch 2.13.0+cu130, scikit-learn 1.9.0 on WORKER_A; the warehouse store
suite in a throwaway venv with duckdb 1.5.5 and SQLAlchemy 2.0.54. Skips named rather than hidden: one sota test needs the
benchmark store receipt that only the coordinator holds; two financial tests (FL08) need the coordinator's governance stack;
the four lake-adopter tests need the coordinator's deployed data-gov configuration and passed there before the hold. No test
ran on the coordinator in this round. The mutation tests — a broken estimator producer, a rewritten report, a fabricated
regeneration, a deletion without prerequisites and a drifted regenerated array — run inside the same suite on the real entry
points.


## Review request

One consolidated audit request: (a) the acceptance chain — whether publishing closure, catalog and regeneration evidence as
governed terminals, and answering from the receipt and the warehouse row, closes the "resolution is not acceptance" gap, and
whether the dated successor acceptances of the two retained reports are scoped honestly; (b) the mandatory deletion
prerequisites and the inode-level unlink boundary, including the residual limit I state rather than claim to have solved;
(c) the corrected author-float32 route, its versioning, and the measured per-cell impact (one number, 3.0e-8); (d) the
per-family domain checks and the independent estimator implementation, including where it could be applied to real bytes and
where the historical limit remains; (e) the T = 96 presentation as measured-but-replay-unverified; (f) the protocol B pilot as
resource evidence and the corrected loader/model facts behind it.


## Errata, 2026-09-23 (added after Musashi's RP121 review; nothing above is deleted)

1. **"every estimator family agrees with an independent recomputation to 0.0" was too broad.** The implementation this return
   described compared residual moments, entropy, mutual information, correlation, R², the errors and the histogram. It did NOT
   compare autocorrelations or quantiles numerically (only their domain), it compared an autocorrelation computed over a
   different population than the catalog's, and it treated a value present on one side and missing on the other as a zero
   difference. Read the claim as covering those families only. RP124 declares the finite inventory, implements the reference for
   every declared family on the same population and definition, and reports coverage per family; the corrected checks were then
   applied to the real cells (see the RP122–RP127 return).
2. **"prerequisites that cannot be skipped" was wrong about the API.** The CLI's defaults were safe, but `delete_predictions`
   and `deletion_preflight` still accepted `require_acceptance=False`, which skipped the accepted-chain requirement. The
   parameter is removed in RP125; tests now supply a faithful accepted chain instead of disabling a production precondition.
3. **"acceptance, not resolution" was incomplete.** `accepted_artifact` verified that the digest belonged to an accepted
   COMPLETED terminal, but read the evidence KIND from the local registry, so a locally relabelled diagnostic attachment could
   present itself as an accepted closure. RP123 reads kind, subject, artifact role and design from the terminal itself; the
   registry is a hint that is reported and ignored when it disagrees.

None of these errata changes a measured score. They change what this return was entitled to claim about the checking of those
scores, and the corrected claims are in the RP122–RP127 return.
