# RP157: retained numbers consistent, two contract boundaries remain incomplete

Reviewed `3c25ecab103163a4face96b6f47ffd8280cb8c67` from the fetched Satoshi
branch. Actual scorer called with subprocess-result fixtures, local temporary
records, source review and published-record comparisons. No inference, live
warehouse, remote process or checkpoint installation was inspected by this audit.

## Findings

1. **High: canonical authentication is still optional and the task is not bound
   to dispatch.** At `tools/df_ecl_modular.py:986-987`, absent or empty
   `identity_covers` skips recomputation altogether. A reduced supplied design
   retaining the run's digest and cell names receives COMPLETE and the claim that
   its digest WAS recomputed. Changing that design's horizon to 192 while dispatch
   and children use 96 still completes. `:1067` also retains local reconstruction
   as fallback. A coverage list supplied by the object cannot define which of its
   scientific fields matter. Require the schema's canonical scientific fields and
   retained accepted design binding, and compare requested versus declared task
   arguments before dispatch. Missing authority is a typed refusal, not a fresh seal.

2. **High: child consensus still substitutes for expected population and typed
   baseline/identity validation.** At `:1039`, the first child's count becomes the
   reference for the others. Setting BOTH population window counts to 1 in ALL
   nine children, leaving their declared element counts unchanged, completes.
   A NaN in `matched_persistence_author_float32.mae` produces a COMPLETE summary
   with NaN naive; only the two model reductions are checked at `:1092-1093`.
   At `:1027`, the string `"false"` is accepted as reconciled identity. Counts must
   be derived from the validated task/support contract, not sibling agreement;
   checkpoint bindings must compare the expected bytes/identities, not a truthy flag;
   validate baseline and derived skill semantics as well as model metrics.

All six one-factor parent-scorer probes reproduce COMPLETE, no problems. They do
not claim those corruptions exist in the real results. See [probe](../evidence/RP157_MUSASHI/probe.py)
and [results](../evidence/RP157_MUSASHI/results.json). No model was run by the probe.

## Accepted and preserved

- New existing tests pass: `test_df_ecl_contrast_gates.py -k 'F2 or F5 or RP157'`,
  CUDA hidden: **19 passed, 6 deselected**. Data-dependent training tests not run.
- Empty factorial, missing/foreign child fields and model metric nonfiniteness
  covered by those repairs remain accepted at their tested scope. The issues above
  are gaps in the same contracts, not a demand for different experimental designs.
- RP157's nine published population records equal RP156's exactly. Independently
  checked declared window/element counts (2,537/78,180,192 and 1,802/55,530,432),
  both model reductions and naive finiteness, literal boolean identity flags,
  and checkpoint digest strings against RP155's records. These checks PASS.
  This is record-level consistency, not new reduction from original arrays or
  authentication against the live accounting service.
- Retain R0 MAE 0.371174, R1 0.374584, R2 0.368596 and paired persistence
  0.868283. R2's approximately 0.694% relative development improvement survives;
  H1, financial utility and an independent matched TimeFilter comparison are not
  established by that observation. No reason for another fit or routine replay.

## One bounded implementation completion, no further inference loop

Satoshi owns the fixes. Finish these existing contracts as a whole before calling
the expensive scorer again. Use the accepted schema/helpers already present; do
not add another registry, model, service or research stage.

| Boundary | Expected authority and positive case | Mandatory refusal cases |
|---|---|---|
| Design | Retained accepted design, required canonical fields, recomputed content and accepted run linkage | absent/empty/altered coverage, missing canonical field, stale or re-digested foreign design, no retained authority |
| Dispatch task | Horizon, seed/regime axes, data/scaler/channel order and monitor support from that validated contract | caller overrides conflicting with design; duplicate, missing or unexpected cell; silent reseal |
| Population | Exact origin/target support and resulting windows/elements per population | all children consistently wrong; inconsistent elements; omitted or extra population; bool/string counts |
| Checkpoint | Exact recorded and observed digest bound to the expected selected model | missing/mismatched digest, truthy string or number in place of literal boolean; wrong cell/model |
| Metrics | Named model AND naive reductions; finite nonnegative losses; recomputed skill with defined zero-naive behavior | NaN/inf/bool/string in any measured numeric field; omitted reduction; contradictory derived skill |

Write these tests first, run them through the real parent closing path, and retain
the valid nine-cell fixture. Extract/use a validation-and-aggregation path that
accepts retained child evidence WITHOUT calling subprocess inference. Closing a
validation bug must not automatically replay all nine models. Reuse existing
RP157 evidence only after its actual authority and population binding pass; publish
an additive closure with the actual scope, cost and references. No new allocation.

## Parallel application delivery

The owner reports Laya SDK installation and snapshot materialization on a worker
have started. Record **SETUP_IN_PROGRESS_REPORTED**, not a measured model pilot.
Do not interrupt or duplicate that job. The Hub name alone is not an immutable
weight version: retain the resolved snapshot commit and file digests before sealing
the pilot. Once setup succeeds, refresh device/RAM/disk admission and execute the
already authorized 600 CPU s / 900 wall s real SDK-to-M5PHET/news-signal pilot.
External5090 remains the preferred eligible GPU; installation host does not by
itself determine execution placement. Respect the planned residency measurement
and actual device identity. A setup failure must name its measured cause.

While installation/downloads run, assign independent CPU tasks in separate
worktrees: collector receipt/revision/dedup persistence, economic-calendar source
inventory/as-of tests, and demo/paper broker refusal/recovery interfaces. None
needs model weights to implement its deterministic boundary. Use available Hermes
agents where useful with one integrator. Real feeds and demo/paper canaries still
require the existing entitlements/risk mandate; no real-capital authorization.

Next return separates (1) closure acceptance using retained evidence, (2) actual
Laya setup/pilot outcome, and (3) concrete commits/tests or exact external missing
objects for those CPU lanes. Finance fitting remains evidence-gated; source and
matched-reference investigation continues. Existing all-lane orders are unchanged.
The installation's status here is reported, not live-observed by Musashi.
