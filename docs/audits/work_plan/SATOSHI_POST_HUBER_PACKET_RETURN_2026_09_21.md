# Satoshi — post-Huber packet: phase 2 sealed, FIN-LOSS-OPT designed, contracts in the runners

Order: [MUSASHI_POST_HUBER_PHASE2](../../handoffs/MUSASHI_POST_HUBER_PHASE2_2026_09_21.md) (points
1–14), incorporating [FIN-LOSS-OPT](../../tres_temas_entrevista/program_v3/FINANCIAL_LOSS_OPTIMIZER_POLICY_2026_09_21.md)
and the mandatory addition to [PROGRAM_METRICS_CONTRACT_v1](../../tres_temas_entrevista/PROGRAM_METRICS_CONTRACT_v1.md).
Base `de88764` → orders merged at `639e079` → this packet at `23223f9`. One review is requested;
nothing here is approved in advance. **This order set measured nothing new: NO_NEW_MEASUREMENT.**

## Owner-facing closure table (point 14)

Generated by `tools/df_closure_table.py` from the three verified run roots — every value recomputed
in float64 from the saved arrays, the naive on the **identical** 10 020 evaluation origins and the
same 60-minute horizon, every terminal digest confirmed in the warehouse (31/31) — and validated by
14 report acceptance tests. Full precision and per-seed rows: [`RP65/CLOSURE_TABLE.json`](../evidence/d3_k5_20260917/RP65/CLOSURE_TABLE.json), [`CLOSURE_TABLE.md`](../evidence/d3_k5_20260917/RP65/CLOSURE_TABLE.md).

Task / target / horizon / split for every row: **UCI 235 household, `Global_active_power`
(minute-averaged active power, kW), 60 steps = 60 min ahead, DEV slice 28 d train / 7 d validation
inside the family train span, test never read.** Metric: MAE in kW; MAE_z = MAE / sd_train
(0.9125164391 kW) in its own column — never compared with kW. skill = 1 − MAE_model / MAE_naive;
positive = smaller error, not accuracy, not profit.

| run (prior, verified) | arm | seeds | MAE kW mean | sd | MAE_z mean | naive MAE kW (same rows) | skill vs naive | literature value & source | comparability |
|---|---|---:|---:|---:|---:|---:|---:|---|---|
| successor RP55 | R0 | 3 | 0.546929 | 0.007803 | 0.599364 | 0.617372 (n=10 020) | +0.114101 | Gasparin 2019, Table 5: GRU-MIMO MAE 0.52 kW — *15-min resample, 96-step day-ahead*; **not placed in comparison** | NOT_COMPARABLE |
| successor RP55 | R1 | 3 | 0.556734 | 0.010761 | 0.610109 | 0.617372 | +0.098219 | same | NOT_COMPARABLE |
| successor RP55 | R2 | 3 | 0.552660 | 0.014515 | 0.605644 | 0.617372 | +0.104818 | same | NOT_COMPARABLE |
| successor RP55 | linear ridge (control) | 1 | 0.545495 | — | 0.597791 | 0.617372 | +0.116425 | same | NOT_COMPARABLE |
| phase 1 RP63 | core_mae | 3 | 0.497770 | 0.000171 | 0.545492 | 0.617372 | +0.193727 | same | NOT_COMPARABLE |
| phase 1 RP63 | core_mse | 3 | 0.546930 | 0.007804 | 0.599365 | 0.617372 | +0.114099 | same | NOT_COMPARABLE |
| phase 1 RP63 | tcn_mse (reference block) | 3 | 0.535948 | 0.005012 | 0.587330 | 0.617372 | +0.131888 | same | NOT_COMPARABLE |
| Huber/AdamW (Musashi) | mae_adam | 3 | 0.496382 | 0.004487 | 0.543970 | 0.617372 | +0.195976 | same | NOT_COMPARABLE |
| Huber/AdamW (Musashi) | mae_adamw | 3 | 0.495073 | 0.004303 | 0.542536 | 0.617372 | +0.198096 | same | NOT_COMPARABLE |
| Huber/AdamW (Musashi) | huber_adam | 3 | 0.520662 | 0.001674 | 0.570578 | 0.617372 | +0.156648 | same | NOT_COMPARABLE |
| Huber/AdamW (Musashi) | huber_adamw | 3 | 0.522479 | 0.005178 | 0.572569 | 0.617372 | +0.153705 | same | NOT_COMPARABLE |

Every row is NOT_COMPARABLE for the same reason, decided from fields before any score: no
literature protocol for UCI 235 matches our target construction, horizon, split and missing policy
(details in §12). The missing comparator is an **open deliverable**: a re-execution of Gasparin's
GRU-MIMO / TCN under our contract (MATCHED_DOMAIN_COMPARISON), designed and costed, not run. Seed
dispersion is optimisation variability on one slice, not sampling uncertainty; units without a
forecast (`prepare`, `ae_*`, cost pilots) are named as skipped, not dropped silently.

## 1. The four-arm comparison incorporated; two overstatements corrected

Musashi's 12-cell factorial is consumed as measured (rows above). The corrections are published
**beside** the RP57–RP64 return, which keeps its labels and numbers:
[`SATOSHI_RP57_RP64_CORRECTIONS`](SATOSHI_RP57_RP64_CORRECTIONS_2026_09_21.md). (i) RP63 changed loss
**and** monitor: its 0.049 kW is a recipe contrast; with the monitor fixed, the loss family accounts
for ~0.024 kW and the optimiser for < 0.002 kW with inconsistent sign. (ii) One shuffled-label fit
is a negative control, not a bound on learnable advantage — "labels buy little" is withdrawn as a
general claim. (iii) A third error of mine: RP61 said a delta head had not been tried; the `tcn_w`
core already has a persistence skip (what it lacks is the zero-initialisation). MAE + Adam is the
household continuity reference only; Huber + AdamW's measured RMSE trade-off stays in the registry.

## 2–8. Phase 2, frozen apart and sealed — not executed

[`RP65/PHASE2_DESIGN_SEALED.json`](../evidence/d3_k5_20260917/RP65/PHASE2_DESIGN_SEALED.json)
(`6b475094…`, 27 cells, `SEALED_NOT_EXECUTED`), produced by `tools/df_e1_phase2_design.py`; acceptance
in `tests/test_df_e1_phase2_acceptance.py` (10 rules) and `tests/test_df_e1_calendar.py` (7 rules).

- **Calendar (3).** Production path `tools/df_e1_calendar.py`: hour and weekday as sin/cos from each
  row's **own** label; clock declared `NAIVE_WALL_CLOCK`, timezone UNKNOWN (local French time
  presumed), no DST transition inside the DEV slice (measured, reported per slice, never assumed);
  publication = static archive. Prefix invariance proven against future values **and** future
  timestamps; a shift-minus-one control fails the same test; numeric, missing, unparseable and
  absent timestamps refused. Control arm Q1.b: the same four channels permuted across rows —
  **equal capacity (9 915 parameters), no calendar information.**
- **Daily context (4).** `core_dilations(W)` derives depth from the window, so a longer window is
  information **and** model — measured at seal: W=60 → 5 blocks, 8 127 parameters; W=1440 → 10
  blocks, 12 047. Q2 separates them factorially: (a) a daily-lag channel y(t+h−1440) — sample
  identity t−1380, available at t, stated; +1 channel = 9 672 parameters; (b) W=1440 with its own
  depth; (c) W=1440 with the core clamped to W=60's five dilations (history present, unreachable);
  (d) W=60 with ten blocks (the model change alone). Train-side evidence for a daily period is stated
  and weak (24 h spectral share 0.067 vs 12 h 0.101; autocorrelation 0.317 at a day vs 0.403 at an
  hour): 24 h is a hypothesis, not an assumed period.
- **Volume (5).** History grown backwards from the same fixed validation week (28 → 56 → 112 days;
  the family train span holds 1 452 681 rows, enough); rows, distinct windows, labels and mean
  exposure per row counted **apart** (28 d: 50 400 rows, 40 080 windows, ≈ 47.7 exposures per row —
  not independent observations); the train-only scaler re-fitted per arm and reported, so a changed
  raw-unit objective is never hidden inside a "volume-only" comparison; a Huber δ in z would move
  with it and is not used in this phase.
- **Adequacy (6).** Seven factorial and four phase-1 fits hit 4 000 updates. Curves every epoch,
  patience 3, restore verified against the best validation MAE; a fit whose best epoch is within one
  of the ceiling is CENSORED; a second tier (8 000) only if declared before the first tier's scores
  are read; more windows under a fixed budget are not more training; no convergence claim.
- **Acceptance first (7).** The runner changes phase 2 needs (calendar/lag channels, a clamped-depth
  builder, backward history growth) are gated by the tests above, the causal battery's negative
  controls (still DETECTED, including `full_series_dwt_as_time_row`), and the unchanged governance,
  update-count, artifact, outbox and warehouse checks of phase 1.
- **Executable plan (8).** Pilot: 200 updates per arm on seed 1, W=1440 arms projected from their
  own pilot; 14 400 s ceiling with 2 000 reserved for closure; stop BUDGET_LIMITED before any
  outcome. Hosts: all arms of one seed on one host (seed = block); the workers only after they run
  inside the accounted wrapper — today their CPU is UNMEASURED (RP56 debt, still open). No cell is
  repeated to fill a machine. Only cheap acceptance fixtures ran in this order set.

## 9–10. FIN-LOSS-OPT — designed, FL01–FL08 in place, NOT_STARTED

[`FIN_LOSS_OPT_DESIGN`](../../tres_temas_entrevista/program_v3/FIN_LOSS_OPT_DESIGN_2026_09_21.md),
sealed [`RP65/FIN_LOSS_OPT_DESIGN_SEALED.json`](../evidence/d3_k5_20260917/RP65/FIN_LOSS_OPT_DESIGN_SEALED.json)
(`0df0ca76…`), `tools/df_fin_loss_opt_design.py`. The task is frozen from the **governed** resource
`financial_files : market_data/forex/g10/eurusd/1h.parquet` (129 873 hourly bars, 2005-01-03 →
2025-12-31, coverage read from the lake; reserve `deny_from 2025-01-01` never read): horizons
**6 h and 72 h declared** (the recalled 6 h / ~3 days are antecedents, not recovered configs), weekly
walk-forward folds, MAE_z with the fold's train σ shared by every arm, persistence naive on identical
rows. Full factorial MAE/Huber × Adam/AdamW with paired initialisations; explicit installed
defaults plus an **equal** bounded search per loss family (12 DEV fits each; LR {5e‑4, 1e‑3, 3e‑3};
AdamW decay {0, 1e‑3, 4e‑3, 1e‑2} on kernels only); Huber δ candidates as fractions of a residual
scale computed **causally** on rolling train origins, re-derived per fold, each with its value in
original units and the fraction of residuals below it — a change after the last train origin cannot
move them (FL06 proves it). Two receivers: compact modular (5 037 parameters, reach 60) and a larger
business-motivated one (W=144), never compared across.

`tests/test_fin_loss_opt_acceptance.py`: **11 green, 5 `xfail(strict)`** that name exactly what only
the financial runner can satisfy (fold-change invariance of scaler/δ/selection; the terminal and
warehouse legs of the 1e‑5/1e‑6 round trip; min_delta and the numeric floor per fold; temporal-block
intervals; governance before the first bar). FL04's arrays → JSON → float64 leg passes today with
sign preserved at 1e‑5 and 1e‑6; FL01's decoupled decay is verified on a zero gradient. Designing
this is not evidence of a winner; the household factorial selects nothing financial.

## 12. Household literature benchmark — prepared, none comparable, nothing run

[`RP65/HOUSEHOLD_LITERATURE_BENCHMARK.md`](../evidence/d3_k5_20260917/RP65/HOUSEHOLD_LITERATURE_BENCHMARK.md)
and [`GASPARIN_2019_REPRODUCTION_CONFIG.json`](../evidence/d3_k5_20260917/RP65/GASPARIN_2019_REPRODUCTION_CONFIG.json).
Four primary sources for UCI 235 were read **at their origin** (arXiv PDFs; the publisher abstract for
the paywalled one) and transcribed field by field:

| source | what it forecasts | why NOT_COMPARABLE with our 60-min minute-power task |
|---|---|---|
| Gasparin, Lukovic, Alippi 2019/2022 (§7.2, Tables 3–5) | 15-min resample, 4-day window, **96-step day-ahead MIMO**, last-year test, same-slot-mean imputation; MAE 0.52–0.57 kW; 10 repeats; no naive; no code | target construction, horizon, split, missing policy |
| Saad Saoud, AlMarzouqi, Hussein 2022 (Tables 1–3) | one step at minutely/hourly/daily/weekly; **SWT computed on the whole series before the split**; 3 y/1 y; no naive; no code | plus **CAUSALITY_UNVERIFIED by construction** (the battery's `full_series_dwt_as_time_row` class); "hourly" is an hourly-aggregated target |
| Vaygan, Rajabi, Estebsari 2021 (Table I) | minute GAP, time pooling, 67/23 within pooled weeks; **horizon never declared**; MAE 0.19 kW | horizon undeclared, pooled split |
| Kim & Cho 2019 (Energy 182) | paywalled; numbers public only as re-executed by a third party | fields unverifiable |

Gasparin's protocol is the only fully specified one: its **REPRODUCTION** lane is designed with
every field fixed, five acceptance checks before any fit (Table 3 counts reproduced on the same
bytes; train-year-only imputation, else labelled FORENSIC; metric formulas; an added persistence
naive; prefix invariance), reference models from Table 4 (GRU-MIMO, TCN), 10 repeats, cost pilot
first. The **MATCHED_DOMAIN_COMPARISON** lane — their GRU-MIMO and TCN re-executed under our contract
— is the only comparator the closure table could ever place in the comparison column. Both wait for
review; a demonstrated causal flaw in a published recipe yields a separately identified corrected
comparison, never a silent replication.

## 13. Literature comparability in the runners — implemented where the next campaigns run, inventoried elsewhere

`tools/df_benchmark_contract.py`: a versioned contract per task; comparability decided from eleven
identity fields **before** any score (a changed target, horizon, split, scaler population, transform,
metric formula or aggregation rejects it — 9 parametrised rules); a known affine conversion
reproduces the metric and a non-affine or unknown one is refused; `require()` refuses a design
without a contract. **Enforced at the real entry points**: `df_e1_phase1.run` and
`df_e1_huber.validate` refuse before any acquisition or fit (proved on the real designs); the
phase-2 and FIN-LOSS-OPT designs embed the contract.
[`RP65/BENCHMARK_CONTRACT_REGISTRY.json`](../evidence/d3_k5_20260917/RP65/BENCHMARK_CONTRACT_REGISTRY.json)
holds our three tasks (household W60_h60, FX EURUSD 1h, RL weekly — RL keeps its own return/risk/
execution contract and no forecast MAE is invented for it) and the four sources.
[`ENTRY_POINT_COVERAGE_INVENTORY.json`](../evidence/d3_k5_20260917/RP65/ENTRY_POINT_COVERAGE_INVENTORY.json):
84 files fit or compile a model, **37 are real entry points, 2 enforce the contract today**; the
legacy plugin/app path and the RL runner are inventoried, not enforced. **All-domain runtime
enforcement is not declared complete.** The precomputed `wavelet.parquet` features the financial
lake serves are not consumed until they pass the battery.

## 11. Traceability, costs, next conditions

Tests added this order set: benchmark contract 17, calendar 7, closure table 14, phase-2 acceptance
10, FL01–FL08 16 (11 + 5 xfail). Focal suites on the committed tree: **190 passed, 5 xfailed**, plus the
two clean-checkout suites (governed route, adoption) 24 passed, the 29 documentary tests and
`check_plan.py` PASS (`scientific_approval: false`). `PROJECT_METHOD_STATE.json`: BENCHMARK-CONTRACTS
`DESIGNED` (the validator's word; the detail records **partial** runtime enforcement, 2 of 37),
FIN-LOSS-OPT `DESIGNED`, MOD-E1 next action = phase 2 after review. Master plan and 09_ADOPCION
carry this packet's section.

CPU this order set on omega: **104.0 s** of 14 400 — acceptance fixtures and sealing only
([`RP65_CPU_LEDGER_omega.json`](../evidence/d3_k5_20260917/RP65/RP65_CPU_LEDGER_omega.json)); the
workers ran nothing and remain unmeasurable until they run inside the wrapper.

**Next execution conditions.** Phase 2: review of this design → cost pilot → campaign, all seeds on
omega unless the workers are brought under the wrapper. Gasparin reproduction and matched
re-execution: after their acceptance tests pass and review. FIN-LOSS-OPT: after its runner exists,
its five xfail rules pass, and the financial delivery's causality gates (RP58's full-configuration
tests) are passed. R0/R1/R2, reserved confirmation and live trading stay outside this block.
