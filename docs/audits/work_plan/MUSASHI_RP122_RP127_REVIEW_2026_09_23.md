# RP122-RP127 independent review

Reviewed: `a73e55b2dcad1265b7643f49fcfda810fcb7d801`.
Disposition: **CHANGES_REQUIRED at the acceptance/deletion integration boundary**.
Preserve scientific measurements and the new independent numerical checks.
No benchmark retraining or blanket regeneration is justified by this review.

## Executed findings

### 1. High: missing numerical verification still permits deletion

`tools/df_sota_repro.py:2576-2604`: `accept_catalog` skips the numerical reference
when `data_path` is omitted, `independent=False`, or arrays are unavailable. It
records no independent comparison and no independently accepted families but
can still emit `pass: true`. `deletion_preflight` at `:1812` checks that boolean,
catalog digest and accepted receipt, not whether the necessary numerical
verification occurred.

Executed on the actual author's disposable CPU fixture with a mutated catalog
producer, not a file edit that merely fails a digest check:

1. Replace all defined ACF values by zero. With the reference enabled, the new
   checker correctly refuses (maximum discrepancy 0.9428690469).
2. Call `accept_catalog(root, design, unit)` without a data path. It emits
   `pass: true`, `independent_comparison: null`, accepted families `[]`.
3. Register that exact diagnostic output in the fixture's accepted chain, make
   a real metadata backup, and call the normal deletion API with no bypass flag.
   It returns COMPLETE and removes the disposable prediction array.

The removed flag is genuinely gone, but the scientific precondition can still
be omitted upstream. Archival/domain-only inspection is useful; it must not be
the same acceptance class that authorizes destructive retention. A terminal
accepting the bytes of a report cannot turn an unperformed check into a check.

### 2. High: the destructive callers do not require the accepted design

`tools/df_sota_repro.py:1807`, `:1821`, `:3219`: deletion and regeneration lookup
omit `design_sha256`. Even callers that provide it allow absent identity through
the `(None, design_sha256)` condition at `:3166`.

Executed with valid local catalog, closure and backup, and honestly rehashed
fixture terminal/receipt pairs declaring a DIFFERENT design:

- Direct lookup with an explicit expected design refuses correctly.
- The actual deletion API accepts the same foreign-design terminal and removes
  the disposable array, because its callers omit that expected identity.
- Removing both design declarations makes direct lookup accept even when the
  expected design is explicitly provided.

This is narrower than the previous local-registry defect, which is repaired.
Kind/role/subject now come from accepted evidence; the design relationship must
be equally mandatory in consumers. Select the authoritative scientific-design
field according to the terminal schema, not by conflating a campaign config
digest with a design digest. Missing and contradictory relationships are not
equivalence. Existing dated history must retain its previous scope.

### 3. Medium: boolean values still compare equal to numeric zero/one

`tools/df_sota_repro.py:2381`: the boolean branch returns equality using `a == b`.
Consequently `False` equals numeric 0 and `True` equals numeric 1, contradicting
the declared typed numeric checks. Mutating the producer's histogram outside
count from 0 to false passes the actual full catalog acceptance, reporting a
zero difference with tolerance zero. It does not change the numeric score in
this fixture, but accepts an invalid metric type. Test this at the consumer,
not only with True versus False in the helper.

## Accepted progress and remaining physical limits

The valid catalog control passes. The corrected numerical checker rejects the
previous false ACF when actually invoked, and the existing tests now cover the
prior quantile/correlation and local-kind cases. The destructive API's old
`require_acceptance` argument has been removed. These improvements stand.

Inspected retained examples in RP127: H96 s2021 has a fully independent numeric
comparison but overall refusal for the missing replay; H720 s2021 passes the
bit-identical regeneration and comparison; H192 s2021 explicitly fails identity
and numeric comparison. This review does not substitute those recorded examples
for a fresh numerical reduction of all twelve production cells.

Reported normalized author-float32 means remain H192 0.157645/0.252163,
H336 0.164480/0.262985, H720 0.190226/0.290617 (MSE/MAE).
They are reported operational agreement, not exact equality or newly measured
values from this audit. No evidence here demonstrates that real scores changed
or that production was deleted through these counterexamples.

H96 remains measured with replay unaccepted. H192 s2021 remains historically
scored, with its extended catalog not independently certified on identical
regenerated bytes. Do not substitute the 5090 output, widen tolerances, discard
the seed or re-fit. Original-device inference/replay is a conditional next
attempt, not a guaranteed cure: preserve any remaining mismatch and its
environment/device scope. Training-device attribution stays historical/inferred.

The owner has not confirmed cooling for either held GPU. No new permission is
needed for the software work, and the two physical holds are not software gates
to bypass. The external 5090 remains the first and only eligible GPU after
admission; no GPU was used in this review.

## Evidence and disposition

[Probe](../evidence/RP127_MUSASHI_REVIEW_2026_09_23/probe.py) and
[executed output](../evidence/RP127_MUSASHI_REVIEW_2026_09_23/PROBE_RESULTS.json)
use disposable CPU files, actual closure/deletion functions and fixture
warehouse responses. No live warehouse mutation, production deletion or restart.
Execution was on WORKER_B, one thread, 3 GiB / 240 s hard scope. Host endpoint
temperatures were at most 50.8 C before and 61.2 C after, not a measured peak.
Suite scope is recorded in [VERIFICATION.md](../evidence/RP127_MUSASHI_REVIEW_2026_09_23/VERIFICATION.md).

Next: [RP128-RP131](../../handoffs/MUSASHI_SOTA_RP128_RP131_2026_09_23.md).
Fix these integration conditions and revalidate retained acceptance evidence;
do not repeat the eleven successful numerical reductions unnecessarily.
