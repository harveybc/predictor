 The training finally have  finished but i have found some problems with it:

 1. We need a better coverage of the optimization events logged in the web interface, because too few events are been logged like current candidate being evaluated/titotal candidates, current generation/total generations per stage and current  stage, when a new stage starts, it must show the fitness reached in each stage up to the last one, fitness of every candidate that gets evaluated (train , validation MAe and Naive MAE, fitness score per candidate, fitness of the champion)

 2. I see that the migration is not working as expected, the requeired funtionallyty is that when an optimum with better performance thatn the best on the network (remember we are using inverse performancwe score, so negative values are better the larger, soitive valuess are worst the larger), i see that somewhere you are aplying a square ar an absolute value or something that messes the comparison of the fitness of the broadcasted candidates and/or the reported fitness, so please veruify that u do not use absolute value never everm, nor squared valaibles for any metrics we are using in our projec, be strict i dont want any manipulations on the selected performance metric, so please do not transform it with absolute value nor square it or anything since it looks that the migration is not perfectly working.

 3. We also now need synchronization per stage, meaning that when one of the participants reaches the end of its stage, it broadcast this event and all the other participants, migrtate to their populations the last champion if not already in its population (the exact same champion parameter values so far) and start the next stage, migrating and accepting/rejecting(+sending last opimum when reject a result to the origin node), in a way that all the participants are always in the same stage.

 4. Plots: the plots in the web interface is not working, i need to  see the perfromance metric in one plot, updated whenever the champion performance is improved(remember tyhat we have configured the experiment to use an inverse performance metric that for example is better when negative and worst when positive), it must be updated every time the champion fitness is improved, that can happen by many events like the node found a new champion, it got the updated champion before reporting new optimum, it requests the neft for the champion whenever a candidate has finished training so  it can incorporate the champion to the population being evaluated, if the cahmpion is not already there, also the champion performance must be updated on the web interface when a stage is finished so it can have the champion in its population if not already there, also when it receives from athe network the end of stage request that makes all nodes to start the the same stage as the one reporting it and keep the stages  synchronized. So whenever the performance of the champion is updated, the plot must be updated to show the hystorty of changes in performance vs time (auto calcualte the vbisible ranges to accomodate all points available.) and not only that but the other plot must show the history of validation MAE (one color), the training MAE(anbother color) and a line clearly marking the Naive of training and validation in their reslpective colors, but darker colors so they can be easily identifiable. I need the history of changes plotted, both the performance metric and the train and validation MAE and  Naive MAe.

 
 5. Rejects: the high rate of rejects makes me believe the nodes are not requesting the champion before submiting their new optimum, also we need better sy7ychronization and as i mentioned latter, i need that after every candidate is evaluated, in all generations ain all stages, the  optimizer requests for the last champion to see if a new champion has been found so it can incorportate it to its current training population and evaluate it during the current generation. Int hat way we ensure that  the champion is always in the populations(please be sure to not to have duplicated champions in the population)

 Please act as a professional data scientist specialized in machine learning development, and fix and /or implement all requirements and please above all avoid all mediocre things like supposing, guessing, or inventing variables anmes , please instead act proffessional and base all your actions on existing code and aread all code that u require.


 Please do a deep analysis of the current logs of the 3 machines that i attach and the current optimization config, butr please make all the modifications in the required files, some of they are attached here .
