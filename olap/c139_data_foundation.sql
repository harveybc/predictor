-- C139 (order 2026-09-12): additive data-foundation grains.
-- GENERATED by tools/load_data_foundation.py ddl(); do not edit by hand.
-- Only CREATE ... IF NOT EXISTS: nothing existing is altered or deleted.

CREATE TABLE IF NOT EXISTS public.df_dim_dataset (
    row_sha256 TEXT PRIMARY KEY,
    run_id TEXT NOT NULL,
    dataset_id TEXT NOT NULL,
    contract_sha256 TEXT NOT NULL,
    content_sha256 TEXT NOT NULL,
    bank TEXT NOT NULL,
    license_state TEXT NOT NULL,
    n_variables BIGINT NOT NULL,
    contract_json JSONB NOT NULL,
    CHECK (run_id IS NULL OR run_id NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (dataset_id IS NULL OR dataset_id NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (contract_sha256 IS NULL OR contract_sha256 NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (content_sha256 IS NULL OR content_sha256 NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (bank IN ('PUBLIC', 'FINANCIAL', 'SYNTHETIC')),
    CHECK (bank IS NULL OR bank NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (license_state IN ('OPEN_ATTRIBUTION', 'OPEN_PUBLIC_DOMAIN', 'RESTRICTED_NO_DERIVATIVES', 'RESTRICTED_NON_COMMERCIAL', 'TERMS_REQUIRE_REVIEW', 'INTERNAL_RESEARCH_ONLY_PENDING_EVIDENCE', 'NOT_APPLICABLE_GENERATED', 'UNKNOWN')),
    CHECK (license_state IS NULL OR license_state NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    loaded_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE IF NOT EXISTS public.df_dim_variable (
    row_sha256 TEXT PRIMARY KEY,
    run_id TEXT NOT NULL,
    variable_id TEXT NOT NULL,
    dataset_id TEXT NOT NULL,
    contract_sha256 TEXT NOT NULL,
    name TEXT NOT NULL,
    unit TEXT NOT NULL,
    semantics_type TEXT NOT NULL,
    role TEXT NOT NULL,
    license_state TEXT NOT NULL,
    CHECK (run_id IS NULL OR run_id NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (variable_id IS NULL OR variable_id NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (dataset_id IS NULL OR dataset_id NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (contract_sha256 IS NULL OR contract_sha256 NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (name IS NULL OR name NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (unit IS NULL OR unit NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (semantics_type IS NULL OR semantics_type NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (role IS NULL OR role NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (license_state IN ('OPEN_ATTRIBUTION', 'OPEN_PUBLIC_DOMAIN', 'RESTRICTED_NO_DERIVATIVES', 'RESTRICTED_NON_COMMERCIAL', 'TERMS_REQUIRE_REVIEW', 'INTERNAL_RESEARCH_ONLY_PENDING_EVIDENCE', 'NOT_APPLICABLE_GENERATED', 'UNKNOWN')),
    CHECK (license_state IS NULL OR license_state NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    loaded_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE IF NOT EXISTS public.df_dim_run (
    row_sha256 TEXT PRIMARY KEY,
    run_id TEXT NOT NULL,
    module TEXT NOT NULL,
    code_sha256 TEXT NOT NULL,
    inputs_sha256 TEXT NOT NULL,
    status TEXT NOT NULL,
    cpu_seconds DOUBLE PRECISION,
    details JSONB NOT NULL,
    CHECK (run_id IS NULL OR run_id NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (module IS NULL OR module NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (code_sha256 IS NULL OR code_sha256 NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (inputs_sha256 IS NULL OR inputs_sha256 NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (status IN ('COMPLETED', 'FAILED', 'INCONCLUSIVE', 'REFUSED', 'REJECTED', 'UNAVAILABLE', 'NOT_RUN')),
    CHECK (status IS NULL OR status NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    loaded_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE IF NOT EXISTS public.df_fact_sampling_quality (
    row_sha256 TEXT PRIMARY KEY,
    run_id TEXT NOT NULL,
    dataset_id TEXT NOT NULL,
    content_sha256 TEXT NOT NULL,
    partition TEXT NOT NULL,
    metric TEXT NOT NULL,
    estimator TEXT NOT NULL,
    estimator_params JSONB NOT NULL,
    value DOUBLE PRECISION,
    value_text TEXT,
    status TEXT NOT NULL,
    reason TEXT NOT NULL,
    code_sha256 TEXT NOT NULL,
    cpu_seconds DOUBLE PRECISION,
    variable_id TEXT NOT NULL,
    CHECK (run_id IS NULL OR run_id NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (dataset_id IS NULL OR dataset_id NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (content_sha256 IS NULL OR content_sha256 NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (partition IS NULL OR partition NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (metric IS NULL OR metric NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (estimator IS NULL OR estimator NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (value_text IS NULL OR value_text NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (status IN ('COMPLETED', 'FAILED', 'INCONCLUSIVE', 'REFUSED', 'REJECTED', 'UNAVAILABLE', 'NOT_RUN')),
    CHECK (status IS NULL OR status NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (reason IS NULL OR reason NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (code_sha256 IS NULL OR code_sha256 NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (variable_id IS NULL OR variable_id NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (status <> 'COMPLETED' OR value IS NOT NULL OR value_text IS NOT NULL),
    CHECK (status = 'COMPLETED' OR (value IS NULL AND value_text IS NULL)),
    loaded_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE IF NOT EXISTS public.df_fact_variable_profile (
    row_sha256 TEXT PRIMARY KEY,
    run_id TEXT NOT NULL,
    dataset_id TEXT NOT NULL,
    content_sha256 TEXT NOT NULL,
    partition TEXT NOT NULL,
    metric TEXT NOT NULL,
    estimator TEXT NOT NULL,
    estimator_params JSONB NOT NULL,
    value DOUBLE PRECISION,
    value_text TEXT,
    status TEXT NOT NULL,
    reason TEXT NOT NULL,
    code_sha256 TEXT NOT NULL,
    cpu_seconds DOUBLE PRECISION,
    variable_id TEXT NOT NULL,
    CHECK (run_id IS NULL OR run_id NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (dataset_id IS NULL OR dataset_id NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (content_sha256 IS NULL OR content_sha256 NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (partition IS NULL OR partition NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (metric IS NULL OR metric NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (estimator IS NULL OR estimator NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (value_text IS NULL OR value_text NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (status IN ('COMPLETED', 'FAILED', 'INCONCLUSIVE', 'REFUSED', 'REJECTED', 'UNAVAILABLE', 'NOT_RUN')),
    CHECK (status IS NULL OR status NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (reason IS NULL OR reason NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (code_sha256 IS NULL OR code_sha256 NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (variable_id IS NULL OR variable_id NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (status <> 'COMPLETED' OR value IS NOT NULL OR value_text IS NOT NULL),
    CHECK (status = 'COMPLETED' OR (value IS NULL AND value_text IS NULL)),
    loaded_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE IF NOT EXISTS public.df_fact_information_metric (
    row_sha256 TEXT PRIMARY KEY,
    run_id TEXT NOT NULL,
    dataset_id TEXT NOT NULL,
    content_sha256 TEXT NOT NULL,
    partition TEXT NOT NULL,
    metric TEXT NOT NULL,
    estimator TEXT NOT NULL,
    estimator_params JSONB NOT NULL,
    value DOUBLE PRECISION,
    value_text TEXT,
    status TEXT NOT NULL,
    reason TEXT NOT NULL,
    code_sha256 TEXT NOT NULL,
    cpu_seconds DOUBLE PRECISION,
    subject_kind TEXT NOT NULL,
    subject_id TEXT NOT NULL,
    CHECK (run_id IS NULL OR run_id NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (dataset_id IS NULL OR dataset_id NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (content_sha256 IS NULL OR content_sha256 NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (partition IS NULL OR partition NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (metric IS NULL OR metric NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (estimator IS NULL OR estimator NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (value_text IS NULL OR value_text NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (status IN ('COMPLETED', 'FAILED', 'INCONCLUSIVE', 'REFUSED', 'REJECTED', 'UNAVAILABLE', 'NOT_RUN')),
    CHECK (status IS NULL OR status NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (reason IS NULL OR reason NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (code_sha256 IS NULL OR code_sha256 NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (subject_kind IN ('VARIABLE', 'MATRIX')),
    CHECK (subject_kind IS NULL OR subject_kind NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (subject_id IS NULL OR subject_id NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (status <> 'COMPLETED' OR value IS NOT NULL OR value_text IS NOT NULL),
    CHECK (status = 'COMPLETED' OR (value IS NULL AND value_text IS NULL)),
    loaded_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE IF NOT EXISTS public.df_fact_pair_relation (
    row_sha256 TEXT PRIMARY KEY,
    run_id TEXT NOT NULL,
    dataset_id TEXT NOT NULL,
    content_sha256 TEXT NOT NULL,
    partition TEXT NOT NULL,
    metric TEXT NOT NULL,
    estimator TEXT NOT NULL,
    estimator_params JSONB NOT NULL,
    value DOUBLE PRECISION,
    value_text TEXT,
    status TEXT NOT NULL,
    reason TEXT NOT NULL,
    code_sha256 TEXT NOT NULL,
    cpu_seconds DOUBLE PRECISION,
    variable_id_a TEXT NOT NULL,
    variable_id_b TEXT NOT NULL,
    lag BIGINT,
    CHECK (run_id IS NULL OR run_id NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (dataset_id IS NULL OR dataset_id NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (content_sha256 IS NULL OR content_sha256 NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (partition IS NULL OR partition NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (metric IS NULL OR metric NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (estimator IS NULL OR estimator NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (value_text IS NULL OR value_text NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (status IN ('COMPLETED', 'FAILED', 'INCONCLUSIVE', 'REFUSED', 'REJECTED', 'UNAVAILABLE', 'NOT_RUN')),
    CHECK (status IS NULL OR status NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (reason IS NULL OR reason NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (code_sha256 IS NULL OR code_sha256 NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (variable_id_a IS NULL OR variable_id_a NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (variable_id_b IS NULL OR variable_id_b NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (status <> 'COMPLETED' OR value IS NOT NULL OR value_text IS NOT NULL),
    CHECK (status = 'COMPLETED' OR (value IS NULL AND value_text IS NULL)),
    loaded_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE IF NOT EXISTS public.df_fact_group_relation (
    row_sha256 TEXT PRIMARY KEY,
    run_id TEXT NOT NULL,
    dataset_id TEXT NOT NULL,
    content_sha256 TEXT NOT NULL,
    partition TEXT NOT NULL,
    metric TEXT NOT NULL,
    estimator TEXT NOT NULL,
    estimator_params JSONB NOT NULL,
    value DOUBLE PRECISION,
    value_text TEXT,
    status TEXT NOT NULL,
    reason TEXT NOT NULL,
    code_sha256 TEXT NOT NULL,
    cpu_seconds DOUBLE PRECISION,
    group_id TEXT NOT NULL,
    members JSONB NOT NULL,
    CHECK (run_id IS NULL OR run_id NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (dataset_id IS NULL OR dataset_id NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (content_sha256 IS NULL OR content_sha256 NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (partition IS NULL OR partition NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (metric IS NULL OR metric NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (estimator IS NULL OR estimator NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (value_text IS NULL OR value_text NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (status IN ('COMPLETED', 'FAILED', 'INCONCLUSIVE', 'REFUSED', 'REJECTED', 'UNAVAILABLE', 'NOT_RUN')),
    CHECK (status IS NULL OR status NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (reason IS NULL OR reason NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (code_sha256 IS NULL OR code_sha256 NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (group_id IS NULL OR group_id NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (status <> 'COMPLETED' OR value IS NOT NULL OR value_text IS NOT NULL),
    CHECK (status = 'COMPLETED' OR (value IS NULL AND value_text IS NULL)),
    loaded_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE IF NOT EXISTS public.df_fact_operator_run (
    row_sha256 TEXT PRIMARY KEY,
    run_id TEXT NOT NULL,
    subject_id TEXT NOT NULL,
    content_sha256 TEXT NOT NULL,
    variable_id TEXT NOT NULL,
    regime JSONB NOT NULL,
    operator_kind TEXT NOT NULL,
    operator_params JSONB NOT NULL,
    spec_sha256 TEXT NOT NULL,
    fitted_sha256 TEXT,
    status TEXT NOT NULL,
    reason TEXT NOT NULL,
    code_sha256 TEXT NOT NULL,
    cpu_seconds DOUBLE PRECISION,
    peak_memory_bytes BIGINT,
    CHECK (run_id IS NULL OR run_id NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (subject_id IS NULL OR subject_id NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (content_sha256 IS NULL OR content_sha256 NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (variable_id IS NULL OR variable_id NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (operator_kind IS NULL OR operator_kind NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (spec_sha256 IS NULL OR spec_sha256 NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (fitted_sha256 IS NULL OR fitted_sha256 NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (status IN ('COMPLETED', 'FAILED', 'INCONCLUSIVE', 'REFUSED', 'REJECTED', 'UNAVAILABLE', 'NOT_RUN')),
    CHECK (status IS NULL OR status NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (reason IS NULL OR reason NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (code_sha256 IS NULL OR code_sha256 NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    loaded_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE IF NOT EXISTS public.df_fact_operator_signal_metric (
    row_sha256 TEXT PRIMARY KEY,
    run_id TEXT NOT NULL,
    operator_run_sha256 TEXT NOT NULL,
    component TEXT NOT NULL,
    partition TEXT NOT NULL,
    metric TEXT NOT NULL,
    estimator TEXT NOT NULL,
    estimator_params JSONB NOT NULL,
    value DOUBLE PRECISION,
    value_text TEXT,
    status TEXT NOT NULL,
    reason TEXT NOT NULL,
    code_sha256 TEXT NOT NULL,
    CHECK (run_id IS NULL OR run_id NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (operator_run_sha256 IS NULL OR operator_run_sha256 NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (component IN ('RAW', 'DENOISED', 'RESIDUAL', 'COMPARISON')),
    CHECK (component IS NULL OR component NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (partition IS NULL OR partition NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (metric IS NULL OR metric NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (estimator IS NULL OR estimator NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (value_text IS NULL OR value_text NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (status IN ('COMPLETED', 'FAILED', 'INCONCLUSIVE', 'REFUSED', 'REJECTED', 'UNAVAILABLE', 'NOT_RUN')),
    CHECK (status IS NULL OR status NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (reason IS NULL OR reason NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (code_sha256 IS NULL OR code_sha256 NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (status <> 'COMPLETED' OR value IS NOT NULL OR value_text IS NOT NULL),
    CHECK (status = 'COMPLETED' OR (value IS NULL AND value_text IS NULL)),
    loaded_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE IF NOT EXISTS public.df_fact_operator_delay_cost (
    row_sha256 TEXT PRIMARY KEY,
    run_id TEXT NOT NULL,
    operator_kind TEXT NOT NULL,
    operator_params JSONB NOT NULL,
    spec_sha256 TEXT NOT NULL,
    metric TEXT NOT NULL,
    frequency DOUBLE PRECISION,
    value DOUBLE PRECISION,
    value_text TEXT,
    status TEXT NOT NULL,
    reason TEXT NOT NULL,
    code_sha256 TEXT NOT NULL,
    CHECK (run_id IS NULL OR run_id NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (operator_kind IS NULL OR operator_kind NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (spec_sha256 IS NULL OR spec_sha256 NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (metric IS NULL OR metric NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (value_text IS NULL OR value_text NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (status IN ('COMPLETED', 'FAILED', 'INCONCLUSIVE', 'REFUSED', 'REJECTED', 'UNAVAILABLE', 'NOT_RUN')),
    CHECK (status IS NULL OR status NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (reason IS NULL OR reason NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (code_sha256 IS NULL OR code_sha256 NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (status <> 'COMPLETED' OR value IS NOT NULL OR value_text IS NOT NULL),
    CHECK (status = 'COMPLETED' OR (value IS NULL AND value_text IS NULL)),
    loaded_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE IF NOT EXISTS public.df_fact_lab_decision (
    row_sha256 TEXT PRIMARY KEY,
    run_id TEXT NOT NULL,
    operator_kind TEXT NOT NULL,
    operator_params JSONB NOT NULL,
    spec_sha256 TEXT NOT NULL,
    regime JSONB NOT NULL,
    decision TEXT NOT NULL,
    rule_sha256 TEXT NOT NULL,
    evidence JSONB NOT NULL,
    failure_regions JSONB NOT NULL,
    externally_reviewed BOOLEAN NOT NULL,
    code_sha256 TEXT NOT NULL,
    CHECK (run_id IS NULL OR run_id NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (operator_kind IS NULL OR operator_kind NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (spec_sha256 IS NULL OR spec_sha256 NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (decision IN ('LAB_CALIBRATED', 'REGIME_LIMITED', 'NOT_IDENTIFIABLE', 'LAB_REJECTED')),
    CHECK (decision IS NULL OR decision NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (rule_sha256 IS NULL OR rule_sha256 NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (code_sha256 IS NULL OR code_sha256 NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (externally_reviewed = FALSE),
    loaded_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE IF NOT EXISTS public.df_fact_snr_calibration (
    row_sha256 TEXT PRIMARY KEY,
    run_id TEXT NOT NULL,
    unit_id TEXT NOT NULL,
    content_sha256 TEXT NOT NULL,
    variable_index BIGINT NOT NULL,
    partition TEXT NOT NULL,
    estimator TEXT NOT NULL,
    metric TEXT NOT NULL,
    value DOUBLE PRECISION,
    value_text TEXT,
    status TEXT NOT NULL,
    reason TEXT NOT NULL,
    code_sha256 TEXT NOT NULL,
    CHECK (run_id IS NULL OR run_id NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (unit_id IS NULL OR unit_id NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (content_sha256 IS NULL OR content_sha256 NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (partition IS NULL OR partition NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (estimator IS NULL OR estimator NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (metric IS NULL OR metric NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (value_text IS NULL OR value_text NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (status IN ('COMPLETED', 'FAILED', 'INCONCLUSIVE', 'REFUSED', 'REJECTED', 'UNAVAILABLE', 'NOT_RUN')),
    CHECK (status IS NULL OR status NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (reason IS NULL OR reason NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (code_sha256 IS NULL OR code_sha256 NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (status <> 'COMPLETED' OR value IS NOT NULL OR value_text IS NOT NULL),
    CHECK (status = 'COMPLETED' OR (value IS NULL AND value_text IS NULL)),
    loaded_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE IF NOT EXISTS public.df_fact_coverage (
    row_sha256 TEXT PRIMARY KEY,
    run_id TEXT NOT NULL,
    dataset_id TEXT NOT NULL,
    variable_id TEXT NOT NULL,
    metric TEXT NOT NULL,
    operator TEXT NOT NULL,
    state TEXT NOT NULL,
    code_sha256 TEXT NOT NULL,
    CHECK (run_id IS NULL OR run_id NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (dataset_id IS NULL OR dataset_id NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (variable_id IS NULL OR variable_id NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (metric IS NULL OR metric NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (operator IS NULL OR operator NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (state IN ('NOT_RUN', 'UNAVAILABLE', 'FAILED', 'INCONCLUSIVE', 'RESULT')),
    CHECK (state IS NULL OR state NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (code_sha256 IS NULL OR code_sha256 NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    loaded_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE IF NOT EXISTS public.df_fact_load_receipt (
    row_sha256 TEXT PRIMARY KEY,
    run_id TEXT NOT NULL,
    table_name TEXT NOT NULL,
    rows_offered BIGINT NOT NULL,
    rows_inserted BIGINT NOT NULL,
    rows_already_present BIGINT NOT NULL,
    rows_refused BIGINT NOT NULL,
    refusals JSONB NOT NULL,
    CHECK (run_id IS NULL OR run_id NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    CHECK (table_name IS NULL OR table_name NOT IN ('PUBLICLY_ELIGIBLE', 'LIVE_ELIGIBLE')),
    loaded_at TIMESTAMPTZ NOT NULL DEFAULT now()
);
